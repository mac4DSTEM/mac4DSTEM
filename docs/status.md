# Status

The one live status table. Updated in the same commit as the work it describes; anything older than the current step moves to `docs/archive/`. Numbers are quoted only from dated runs. Releases: `docs/releasing.md` § Releases.

**What a log name in these rows is** (settled 2026-09-09): a log name like `unit-c7s2-20260908.log` identifies the run a number came from, not a path a reader can open — session logs live in the gitignored session scratchpad and are not retained past the session. What a reader reproduces is the gate, not the log — `tools/run-tests.sh` is the only thing that knows the harness count. Evidence a reader must be able to open is committed under `docs/archive/`, and the inventory gate fails on a repo-rooted path a truth doc cites and does not have.

## Where the UI stands

What that train left behind is the shape the app has now — `DSTEMCore` and `DSTEMSession` packages, `DisplayedProduct`, `CalibrationSession`, `ProductWorkflow.readiness`, `ACOMSession`, six workspace sidebars. The dated step table is archived at [`archive/v3/status-steps-2026-09-16.md`](archive/v3/status-steps-2026-09-16.md).

## Last gates (retained logs)

| Gate | Result |
|---|---|
| `run-tests.sh all` (v4.0.0 cut) | **exit 0 — 2026-09-22/23** on `1b76c98` (`all-v4-20260922.log`, `GATE_EXIT=0` on its own line): 50 harnesses, zero `FAIL` lines, real-data acceptance and package-test (4.0.0 (7), floor 27.0, arm64 alone) included; unit **860 passed / 0 failed / 1 skipped = 861**, reconciled with 861 `func test`. |
| Al-Mg-Si lost-matrix Gate D (2026-09-24; pre-registered `376dbe7`) | Cause: the **file's calibration**. Q is 1.733× too large (data 0.02639 Å⁻¹/px), plus an unmodelled ellipse (1.085, 69.6°); the specimen is on **[001]Al, not ⟨110⟩**. H1–H4 refuted as registered, H5 held: matrix 0.6 → 91.1 % with both corrected. Independent refuter: numbers reproduced exactly; one test withdrawn (a detector-corner artefact). New diagnostic `tools/matrix-orientation-probe/` (logs `gd-015`, `gd-h4a/b`, `gd-h5`, `gd-05`, all exit 0). No app code changed; Gate D closed. Record `archive/v4/almgsi-gateD-2026-09-24.md`. |
| macOS 27 migration audit (2026-09-24) | Clean scratch build: exactly two warnings (a deprecated `boundingRect`, an unused `take()`), both fixed; three test-target warnings fixed (one a real isolation slip: `LockedFractions` called off the main actor). No `#available` left; all four pbxproj targets and `Package.swift` at 27.0. Unit **882 passed / 0 failed / 1 skipped = 883**, reconciled with 883 `func test` (`unit-migration3.log`, `UNIT_EXIT=0`), **0 compiler warnings**. No number moves; Gate D not applicable. UI survey: no migration debt; 58 `String(format:)` display sites ignore the locale (`open-items.md`). |
| Al-Mg-Si drive (2026-09-24 night; pre-registered `bac36e4`) | Demo cube: A1 held exactly (6 + 3 β″ objects, search rule); A2 held (known variants labels grain B β″). Real cube: B1 held (⟨110⟩, 43 %); B2/B3 FAILED at 0.15 % and 0.5 % (matrix 0.7 / 2.2 %, β″ speckle). Record `archive/v4/almgsi-drive-2026-09-23.md`. Two fixes: duplicate phase names get their zone axis; density on its own line. Unit `unit-final3.log`, core `core-final3.log`, inventory `inventory-final3.log` (numbers in the commit message). |
| Known-variants guard (Gate D + Gate B) and the precipitate objects finish, ADR 038 (2026-09-23 night) | Unit **881 passed / 0 failed / 1 skipped = 882**, reconciled with 882 `func test` (+13: 6 guard, 5 report, 1 stale-token, 1 CIF name) (`unit-final.log`, `UNIT_EXIT=0`); core exit 0 (`core-final.log`); inventory exit 0 (`inventory-final.log`). Every new test broken first (14 mutations red, `mutations.txt`, `rmutations.txt`, `nmutations.txt`). Gate D: the app's guard gives label maps identical to the measured inline guard at 0.1 / 0.15 / 0.2 / 0.5 % and at one pixel (`tools/thronsen-dataset/compare_labels.py`, 7/7 IDENTICAL; anti-vacuity differs at 75). Prediction REFUTED at 0.5 % (3333 → 3338). Gate B refuter: numbers NOT REFUTED; three surviving mutations, each now tested. **Driven on screen** (build `dd-app`, `datasetA_stride3.h5`, 0.15 % floor, known variants): Precipitates section, minimum size 10 px (θ′ 3 objects, T1 43), Show Objects, Object Table with sort and Counted Only. The drive found identical "VESTA_phase_1" names, missing swatches, `%.3g` exponents and an ISO subtitle, all fixed and re-driven. Unverified: the last column widths. |
| Relative detection floor sweep 0.1 / 0.15 / 0.2 % (2026-09-23 night, owner's run; prediction committed first, `767d484`) | Three `tools/thronsen-dataset/run.sh probe … --object-table --dump-labels` runs, each exit 0 on its own line (`floor-exits.txt`); `direction_check.py app` exit 0 ×3. **1.81 → 1.45 → 4.07 %** (529 / 424 / 1189 of 29 241). The 0.1 % rerun is identical to the baseline. The prediction was REFUTED at 0.15 % (error fell, not rose) and on edge-on area fraction (−17/−22 %); every other direction held. At 0.15 % the app's T1 raw speckle is 5 spurious objects, inside the published 1–13 (23 at 0.1 %); with the guard, 1.31 %. One dataset, three points. No code changed; Gate D not applicable. `run-tests.sh inventory` exit 0 (`inventory-floor.log`). |
| T2–T4 of `docs/cloud/2026-09-23-brief.md`, run locally (2026-09-23 night; the cloud proxy refuses Zenodo) | `direction_check.py report` / `app` exit 0 (`report.log`, `app.log`); `tools/thronsen-dataset/run.sh probe … --object-table --dump-labels` exit 0 (`probe-dump.log`, 1.81 % and 101/70/62 unchanged); `run-tests.sh inventory` exit 0 (`inventory-t2t4-final.log`). The paper's per-method errors are reproduced to the digit. The Python object reference matches the Swift `--object-table` 9/9 rows on the real map. Independent refuter (Sonnet, own scipy recount): every number NOT REFUTED; interpretation PARTLY REFUTED (cleaned counts are blind to scattered error, and the fair T1 reference is 34, not 37), and T2–T4 were rewritten to match. New: `tools/cloud-analysis/` (diagnostic; the PR #2 reference plus four scripts); the probe's off-by-default `--dump-labels`. Gate D not applicable: no `mac4DSTEM/` source and no app number changed. |

Earlier gate rows — 2026-09-17 through the 2026-09-23 overnight runs — are archived verbatim at [`archive/v4/status-history-2026-09-23.md`](archive/v4/status-history-2026-09-23.md).

## Handoff — 2026-09-23

| Item | State | Next step, owner |
|---|---|---|
| **Now** | v4.0.0 released 2026-09-23 (4.0.0 / 7, macOS 27+): calibration foundation + rebuilt UI, notarized app + DMG (`releasing.md`). | Next cut. |
| **Owner decisions owed** | (1) the object-level pass bar (`docs/cloud/2026-09-23/T4-object-preregistration-DRAFT.md`) and the `lengthPx` definition; (2) R–Q displayed sign convention — app θ = −py4DSTEM's on real data; decide which is displayed, then Gate D the sign conversion (`open-items.md`); (3) Al lattice constant — 4.0495 Å (pure Al) vs 4.04 Å (the paper's CIF); correct Finding 4's `kMax`-knife-edge mechanism if a `DEVIATION` note is written; (4) CI needs a macOS 27 runner image — `macos-26` can no longer build the app at all. Carried: parallax bin-schedule; cross-phase completeness guard; release-readiness Gate D items; 119-defect triage; collapsible cards (kept for now). | Owner. |
| **Materials Project** | Shipped in v4.0.0 (importer landed 2026-09-21). Owner live-tested the fetch 2026-09-23: it worked. | S6's pre-registered comparisons: Al/θ′/T1 cells vs the paper's CIFs, a phase map through them within the S1 band, mp-1185307 refused. |
| Phase mapping / AI Analysis | Unvalidated by design. Known-variants 1.81 % (default) → 1.45 % guarded; not parameter-free (refuter). Object check (`docs/cloud/2026-09-23/` T2–T4): published maps at ~1 % are as far off raw; cleaned counts cannot certify a classifier; app T1 raw speckle outside the published range. | Guard + objects shipped (ADR 038). Al-Mg-Si Gate D closed: the file's calibration (`open-items.md`). **Next:** owner's three questions there; orientation coverage; the object bar. |
| R–Q rotation | ADR 024's null still refuses `sim_Au` by design. The frame-class close was reopened 2026-09-23: independently PARTIALLY REFUTED — the harness fix relabels py4DSTEM into the app's frame rather than proving the sign right. | Owner: displayed convention (above), then Gate D the sign conversion. |
| **Unverified on screen** | Light mode on a real cube; the table's final column widths; density on its own line. Objects driven on Thronsen and the demo cube (6 + 3, exact); the Al-Mg-Si cube awaits a corrected re-drive. | — |
| Window design residuals | Closed 2026-09-22 night (ADR 008, 035–037). Open: the toolbar's leading jump at a long file name (915 pt floor); inspector-kit gaps (adaptive `Menu`, warning-note type); a transient panel-blank lead, parked not diagnosed. | `open-items.md`. |
| Orientation coverage | Monoclinic 2/m unlocks β″; a separate Gate D/B feature once the owner confirms it goes next. | Owner confirmation. |
| Detector kernel | Detection gains worsen the matrix-by-exclusion verdict. | After the rows above. |

Text of record for the 2026-09-16/17 handoff: [`archive/v3/status-handoff-2026-09-18.md`](archive/v3/status-handoff-2026-09-18.md); the day's evidence stays in `open-items.md` and `archive/v3/`.

## Owed to the owner

- The §10g decisions and plan §8 (sidecar wire format). C8's engines question was settled 2026-09-08: leave (`decisions.md`).
- Four session-4 choices to overrule on sight, and the C4 slices 1-2 drive — both carried in [`archive/v3/v3.0.0-closeout-2026-09-11.md`](archive/v3/v3.0.0-closeout-2026-09-11.md).
