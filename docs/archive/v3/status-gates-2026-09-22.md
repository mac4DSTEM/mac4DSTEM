# Status gate rows, 2026-09-22 afternoon–evening (moved from `docs/status.md`)

Superseded by the night's UI/UX rebuild; kept verbatim as the record.

| Gate | Result |
|---|---|
| `run-tests.sh unit`; `core`; `inventory`; scratch app build (2026-09-22 afternoon, Settings · Info as one Liquid Glass capsule with text segments — his Xcode crop; the inspector column pinned to the top — his launch finding) | Unit **854 passed / 0 failed / 1 skipped = 855**, reconciled with 855 `func test` (`unit-capsule-20260922.log`, `UNIT_EXIT=0`; no test added — a view row and a frame, nothing to mutate; the launch state is the evidence). Core exit 0 (`core-capsule-20260922.log`). Inventory exit 0 (`inventory-capsule-20260922.log`). Scratch build exit 0 (`build-capsule2-20260922.log`). Agent captures on a marked instance: `before-launch-tab-row-mid-column.png` (his finding reproduced at `3f7227f`), `after-launch-tab-capsule.png` and `after-prepare-tab-capsule.png` (the capsule at the top in both states). Gate D not applicable: presentation only; the launch defect's mechanism is read in the code and reproduced in the before capture. **Unverified on screen by the owner.** |
| `run-tests.sh unit`; `core`; `inventory`; scratch app build (2026-09-22 afternoon, Prepare back on the rooms' `InspectorRows` kit — ADR 037 reversed at his word) | Unit **854 passed / 0 failed / 1 skipped = 855**, reconciled with 855 `func test` (`unit-prepare-kit-20260922.log`, `UNIT_EXIT=0`); the file is `69c40d1`'s with the six-step readiness line kept, so its tests are the ones that already broke under mutation (`prepare-M-five-steps-20260922.log`). Core exit 0 (`core-prepare-kit-20260922.log`). Inventory exit 0 (`inventory-prepare-kit-20260922.log`). Scratch build exit 0 (`build-prepare-kit1-20260922.log`). Agent capture on a marked instance (`after-prepare-back-on-the-kit.png`: the same section headers as Bragg disks). Gate D not applicable: presentation only, every scientific row carried over. **Unverified on screen by the owner.** |
| `run-tests.sh unit`; `core`; `inventory`; scratch app build (2026-09-22 afternoon, Settings · Info as text tabs in Liquid Glass after his drive of `03cfa10`) | Unit **854 passed / 0 failed / 1 skipped = 855**, reconciled with 855 `func test` (`unit-glass-20260922.log`, `UNIT_EXIT=0`; no test added — the row is two standard buttons and an availability branch, nothing to mutate). Core exit 0 (`core-glass-20260922.log`). Inventory exit 0 (`inventory-glass-20260922.log`). Scratch build exit 0 (`build-glass2-20260922.log`). Agent capture on a marked, inactive window (`after-glass-text-tabs.png`: the prominent tab reads grey there; tinted when the window is key). Gate D not applicable: presentation only. **Unverified on screen by the owner.** |
| `run-tests.sh unit`; `core`; `inventory`; scratch app build (2026-09-22 late, the owner's §9 drive built the same night: icon tabs, the 34-pt infobar with the live run, Output · Lineage panes, `GroupBox` cards, Settings toggle removed; ADR 034/036/037 amended) | Unit **854 passed / 0 failed / 1 skipped = 855**, reconciled with 855 `func test` (`unit-late-20260922.log`, `UNIT_EXIT=0`; one outcome line lost its `Test case` prefix to interleaved output — count by the `' passed on` suffix, never the prefix); +4 −2 (pane rule, default panes, run line, readout slot; the tab enum's two gone). Pane rule red under a mutation that never closes the area (`late-M-pane-rule-20260922.log`), run line red without the rate (`late-M-runline-no-rate-20260922.log`); the slot test went red by itself at 360 pt (`late-clean-20260922.log`) and green at the measured 396 (`late-clean2-20260922.log`, 45 of 45). Core exit 0 (`core-late-20260922.log`). Inventory exit 0 (`inventory-late-20260922.log`, handoff 447/450, AppState + ResultExport 3204 = HEAD^). Scratch build exit 0 (`build-late3-20260922.log`). Agent captures of three states on a marked instance (`after-late-*.png`). Gate D not applicable: placement and presentation only. **Unverified on screen by the owner.** |
| `run-tests.sh unit`; `core`; `inventory`; scratch app build (2026-09-22 evening, the inspector's toolbar: picker + one toggle, ADR 036 corrected) | Unit **852 passed / 0 failed / 1 skipped = 853**, reconciled with 853 `func test` (`unit-toolbar2-20260922.log`, `UNIT_EXIT=0`). Core exit 0 (`core-toolbar2-20260922.log`). Inventory exit 0 (`inventory-toolbar2-20260922.log`, handoff 444/450). Scratch build exit 0 (`build-toolbar7-20260922.log`). **Both panel states captured by the agent with the new launch flags** (`after-toolbar-036-inspector-shown.png`, `after-toolbar-036-panels-hidden.png`): one toggle in each, the picker only with the column. Owner rebuild and check owed. |
| `run-tests.sh unit`; `core`; `inventory`; scratch app build (2026-09-22 evening, Prepare as cards, ADR 037) | Unit **852 passed / 0 failed / 1 skipped = 853**, reconciled with 853 `func test` (`unit-prepare-20260922.log`, `UNIT_EXIT=0`); +1 `CalibrationDisclosureTests` (the readiness row counts six steps), red under a five-step mutation then green (`prepare-M-five-steps-20260922.log`, `prepare-clean-20260922.log`, 8 of 8). Core exit 0 (`core-prepare-20260922.log`). Inventory exit 0 (`inventory-prepare-20260922.log`, handoff 444/450, AppState + ResultExport 3204 = HEAD^). Scratch build exit 0 (`build-prepare1-20260922.log`). Agent capture only (`after-prepare-cards-037.png`, dark, inactive window, demo fixture). Gate D not applicable: presentation only, every scientific row carried over. **Unverified on screen by the owner.** |
| `run-tests.sh unit`; `core`; `inventory`; scratch app build (2026-09-22 evening, the toolbar carries the room, ADR 036) | Unit **851 passed / 0 failed / 1 skipped = 852**, reconciled with 852 `func test` (`unit-toolbar-20260922.log`, `UNIT_EXIT=0`); +1 `StatusBarMetricsTests` (the toolbar's idle display), red under a word-order mutation then green (`metrics-M-room-first-20260922.log`, `metrics-clean-20260922.log`). Core exit 0 (`core-toolbar-20260922.log`). Inventory exit 0 (`inventory-toolbar-20260922.log`, handoff 437/450, AppState + ResultExport 3204 = HEAD^). Scratch build exit 0 (`build-toolbar4-20260922.log`). **Driven by the owner on his own build the same hour** — three corrections on his word, "better now" after the first; his check of the last two (the verb's side, one toggle when hidden) owed. Gate D not applicable: placement only. |
