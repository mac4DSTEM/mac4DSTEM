# Status

The one live status table. Updated in the same commit as the work it describes; anything older than the current step moves to `docs/archive/`. Numbers are quoted only from dated runs. Releases: `docs/releasing.md` § Releases.

**What a log name in these rows is** (settled 2026-09-09): a log name like `unit-c7s2-20260908.log` identifies the run a number came from, not a path a reader can open — session logs live in the gitignored session scratchpad and are not retained past the session. What a reader reproduces is the gate, not the log — `tools/run-tests.sh` is the only thing that knows the harness count. Evidence a reader must be able to open is committed under `docs/archive/`, and the inventory gate fails on a repo-rooted path a truth doc cites and does not have.

## Where the UI stands

What that train left behind is the shape the app has now — `DSTEMCore` and `DSTEMSession` packages, `DisplayedProduct`, `CalibrationSession`, `ProductWorkflow.readiness`, `ACOMSession`, six workspace sidebars. The dated step table is archived at [`archive/v3/status-steps-2026-09-16.md`](archive/v3/status-steps-2026-09-16.md).

## Last gates (retained logs)

| Gate | Result |
|---|---|
| `run-tests.sh unit`; `core`; `inventory`; scratch app build (2026-09-22 night, UI/UX rebuild on macOS 27: floor 14→27, toolbar priorities, derived 915-pt window floor, the flat HIG inspector kit in every room, contrast in the pane popovers, Computed in Info, the kernel source picker, two Gate D fixes) | Unit **860 passed / 0 failed / 1 skipped = 861**, reconciled with **861** `func test` (864 − 4 retired window-budget tests + 1 new floor test, red at 640) on HEAD `3514566` (`unit-hdr2-20260922.log`, `UNIT_EXIT=0`; the run before it refused on the 4 GB floor, exit 69). Core exit 0 (`core-macos27-20260922.log`, the floor bump). Inventory exit 0. Functional audit vs `9ded6b4`: all 200 identifiers, 36 run actions, 20 shortcuts, 4 importers, 7 dialogs and every help text present. Gate D ×2: the pane clipped under the inspector (strip slots floored the detail column; refuter NOT REFUTED) and one unreproduced constraint-loop crash (slots now constant min/ideal/max). **Owner-driven on a real cube 2026-09-22 ~23:00** ("fantastic so far"); his two findings (pill, image offset) fixed after (`2d47632`, `3514566`), unseen by him. ADR 008 rewritten, 035/036 amended, 037 superseded. |
| `run-tests.sh unit`; `core`; `inventory` (2026-09-22 night, consolidation execution: six commits `ce65014`..`10e765a`) | Unit **862 passed / 0 failed / 1 skipped = 863** shown, reconciled to **864** `func test` (`final-unit-20260922.log`, `EXIT:0`; one result line interleaved with concurrent xcodebuild-worker output — this file's documented trap, count by the `' passed on` suffix). Core exit 0 (`final-core-20260922.log`). Inventory exit 0 (`final-inventory-20260922.log`). Landed: PaneOverlays.swift dead code + IPF-legend merge; a storage-key rename; a Settings/Phase cross-reference; the ACOM Source row (new test, broken first); the calibration-readiness-row + detection-warning merge; a Gate D (`/diagnose`) + Gate B (independent agent) parallax stage-4 fix (7 new tests). Findings and every commit or block reason: `archive/v3/consolidation-review-2026-09-22.md`. **The session's own task premise — a prior review landed at commit `4ff4884` — does not exist anywhere in this repo** (`git fsck`, doc and scratchpad search, all before any code changed); every claim was independently re-verified against source, never taken on that citation's authority. Three residuals blocked by Frozen Shell (ADR 035): a `WorkspaceInspector.swift` storage-key fossil, the `PhaseSettings.swift` rename, the Info-tab Unvalidated badge. **Unverified on screen** — the owner was driving the app live during this session (three findings recorded below); building/launching a second copy to self-verify was skipped to avoid disturbing his running session. |
| `run-tests.sh unit`; `core`; `inventory`; isolated app build (2026-09-22 frozen shell, ADR 035) | Unit **850 passed / 0 failed / 1 skipped = 851**, reconciled with 851 `func test` (`unit-frozen-shell-20260922.log`, `UNIT_EXIT=0`; one result line interleaved with Xcode timing output, so the suffix counter reads 849); +3 `NavigationSeamTests` and one rewritten from the maxima budget, each red under one production mutation (`seam-A/B/C-*-20260922.log`, `seam2-B-*.log`) and green clean (`seam2-clean-20260922.log`, 10 of 10). Core exit 0 (`core-frozen-shell-20260922.log`). Inventory exit 0 (`inventory-frozen-shell-20260922.log`, handoff 439/450, AppState + ResultExport 3204 = HEAD^). Scratch app build exit 0 (`build-review6-20260922.log`). **Seen on screen by the agent** (`verify5-20260922.log`, demo fixture, dark): inspector kept at 1100 pt and toggle enabled by accessibility readout; no toolbar title, toggles trailing; infobar dragged to fraction 1 and back with both panes drawn; row-resize pointer captured. Gate D not applicable: view state and placement only. **Owner acceptance owed.** |
| `run-tests.sh unit`; `core`; `inventory`; isolated app build (2026-09-22 window anatomy, phase 1) | Unit **847 passed / 0 failed / 1 skipped = 848**, reconciled with 848 `func test` (`unit-window-anatomy-cut-20260922.log`, `UNIT_EXIT=0`); +3 `NavigationSeamTests`, each red under one production mutation before restoration. Core exit 0 (`core-window-anatomy-cut-20260922.log`, `CORE_EXIT=0`; first sandboxed attempt lacked SwiftPM cache permission). Closeout inventory exit 0 (`inventory-window-closeout-20260922.log`, handoff 427/450). Isolated app build exit 0 (`build-window-anatomy-cut-20260922.log`). Gate D not applicable: window placement and view state only; no scientific number moves. **Unverified on screen.** |
| `run-tests.sh unit`; `core`; `inventory` (bottom workspace + utility-pane inspector, ADR 034; the Settings slice before it) | **all exit 0 — 2026-09-21** (`unit-workspace-final-20260921.log`, `UNIT_EXIT=0` on its own line): unit **844 passed / 0 failed / 1 skipped = 845**, reconciled against 845 `func test` — +13 on the Settings slice (`unit-settings-20260921.log`, 818/0/1 = 819: `AppPreferencesTests`, one `RecentDatasetsTests`, four `OperationCenterKeepAwakeTests`) and +26 here (20 `BottomWorkspaceTests`, 6 `InspectorRowsTests`), every new test broken one mutation at a time; `core-workspace-final-20260921.log` `CORE_EXIT=0`; `inventory-workspace-final-20260921.log` `INVENTORY_EXIT=0`, handoff 426/450, AppState + ResultExport 3204 (3198 at HEAD: the read-only `replaySteps` forwarder, which no other file could hand the Lineage tab). Reviews (Sonnet finders, three Sonnet refuters per finding, majority): Settings slice 7 found / 6 confirmed / 3 code fixes; workspace diff 16 found / 16 confirmed / 15 fixed, one accepted (the Imaging radius field rounds where the old caption truncated). Gate D not applicable — placement, presentation and live-state plumbing; no scientific number moves. Nothing driven on screen. |
| `run-tests.sh unit`; `core`; `inventory` (2026-09-21 evening: known-variants rule, MP importer, S3 wiring, per-phase slab) | **all exit 0 — 2026-09-21**: unit **804 passed / 0 failed / 2 skipped** on the per-phase-slab tree (the S1 agent's `unit-s1-final.log` 794/0/2 = 796 reconciled against 796 `func test`; S3 `unit-s3.log` 802; slab run 804), core exit 0, inventory exit 0 (`inventory-closeout-20260921.log`, handoff 448/450). Thronsen truth: control 4.21 % reproduced before every experiment; known-variants at floor 0 with θ′ at slab 0.3 **1.81 %** (`map-known-variants-phaseslab-20260921.log`). Records: `archive/v3/known-variants-rule-2026-09-21.md`, `t1-relationship-2026-09-21.md`, `theta-prime-slab-2026-09-21.md`. |
| `run-tests.sh unit`; `run-tests.sh core`; `run-tests.sh inventory` (class-map bridge + retained Session owner) | **all exit 0 — 2026-09-21** (`unit-classification-owner-20260921.log`, `UNIT_EXIT=0`; `core-classification-owner-20260921.log`, `CORE_EXIT=0`; `inventory-classification-owner-20260921.log`, `INVENTORY_EXIT=0`): unit **749 passed / 0 failed / 2 skipped = 751**, reconciled against 751 `func test` declarations (+2 `PrecipitateClassificationProductTests`; one result line interleaved with Xcode timing output, so its suffix counter reads 748). `DSTEMCore` + `DSTEMSession` build clean; inventory handoff 438/450 words. The owner’s `clear` mutation was red (exit 65), then restored. Gate D does not apply: this slice only owns an existing Core value; the bridge’s prior Gate D/B record is [`archive/v3/phase-map-objects-gateD-2026-09-21.md`](archive/v3/phase-map-objects-gateD-2026-09-21.md). |
| `run-tests.sh all` (v3.1.0 readiness) | **exit 0 — 2026-09-21** (`all-v310-20260921.log`, `GATE_EXIT=0` on its own line, HEAD 82e683a, 11:20–11:32, 23 GB free): unit **742 / 0 / 2 = 744**, reconciled per file and per class against 744 `func test` by an independent refuter; 50 harnesses = the 48 `scientific` names once each + `real-data-acceptance` (all 5 pinned cubes measured and matched at rel_tol 2e-6, 3 unpinned announced, 35 s) + `package-test` (arm64-only, hardened, **3.0.0 (6)**, 96 s); zero `FAIL` lines. **Not yet the cut gate:** the bundle it certified is 3.0.0 (6); `all` re-runs on the bumped tree. Trap learned: under Xcode 27 `-quiet`, "`error: the following command failed with exit code 0 but produced no further output`" is the header printed before a zero-exit compile step's WARNINGS — here `App/AppState+Promote.swift:26`, result of `take()` unused (hygiene; fix rides with the bump) — not a failure. |
| focused XCTest; `run-tests.sh unit`; `tools/friedel-origin-test/run.sh` (Friedel row progress) | **all exit 0 — 2026-09-19** (`unit-progress-20260919.log`, `UNIT_EXIT=0`): 742 passed / 0 failed / 2 skipped = 744, reconciled against 744 `func test`. The row-progress test was red before its source mutation (0 interior updates), then green; unit and py4DSTEM parity retain the numerical result. On-screen 171×171 re-drive remains owed. |
| `run-tests.sh unit` (AppState seam 7, the last seam — plan complete) | **741 passed / 0 failed / 2 skipped = 743 — 2026-09-18** (`unit-seam7-fixed.log`, `SCRIPT_EXIT=0`; 743 names reconciled against 743 `func test`). First run **failed**: 740/1/2=743, `PromotionCommitTests.testCommitRefusalPreservesThePendingLoadForCorrection` red (`unit-seam7.log`) — the moved `commitPendingLoad` cleared `promotionRun` before its refusal guard finished, so a beam-excluding crop silently dropped the configurator's pending load instead of leaving it open for correction; fixed (`App/AppState+Promote.swift`, inline `DEVIATION` note), re-run green. Previous: 732/0/2=734 — 2026-09-18, AppState seam 6 (`unit-seam6-20260918.log`). All five `DatasetSessionTests` went red under their production mutations, then clean-green. Targeted `ReplayExecutionTests` and the cdhash error-detail regression also passed. |
| `run-tests.sh inventory` (AppState seam 7, the last seam — plan complete) | **exit 0 — 2026-09-18** (`inventory-seam7.log`, `SCRIPT_EXIT=0`): AppState.swift **1474** (was 3054) — no longer the repo's largest file (`Support/ResultExport.swift` is, at 1601); AppState + ResultExport **3075** (was 4655); handoff **408** words (cap 450). Previous: exit 0 — 2026-09-18, AppState seam 6 (`inventory-seam6-final-20260918.log`): AppState + ResultExport 4655 (4841 at HEAD); AppState.swift 3054; live markdown 5816. |
| `run-tests.sh unit` | **720 passed / 0 failed / 2 skipped = 722 — 2026-09-18, AppState seam 4 (`DPCProduct`, the last unattended seam of the night)** (`unit-seam4-20260918.log`, `UNIT_EXIT=0` on its own line, 722 names reconciled against 722 `func test`; +5 over seam 3, the new `DPCProductTests`). Previous: 715 / 0 / 2 = 717 — 2026-09-18, AppState seam 3 (`unit-seam3-20260918.log`). Before that: 708 / 0 / 2 = 710 (seam 2); 701 / 0 / 2 = 703 (seam 1); 696 / 0 / 2 = 698, the AppState-split tree (`unit-split-20260918.log`, `UNIT_EXIT=0` on its own line, 698 names reconciled against 698 `func test`). Previous: 693 / 0 / 2 = 695, 2026-09-17 (`unit-v31-full2.log`, `UNIT_EXIT=0` read off the log's own line, not the background wrapper — the caller's "exit 0" is untrusted). 695 `func test` in source, reconciled by the suffix count (`grep -o "()' passed on 'My Mac"`, this file's documented trap); **+4** over the BraggVectorEMDTypes tree (2 `CalibrationDisclosureTests` init-carry + nil-contract, 2 Metal-driven `ProbeSizeTests` production-carry), **−1** tautology removed after the adversarial review; the same two environment skips (`BookmarkResolutionLatencyTests`, `TB1StallProbeTests`). Break-first M1 (init), M2 (`appOriginMaps` bridge) and **M3 (both production carries → nil)** each went red then clean-green (exit 65); M3 is the one the first tests could not catch. `core` + `inventory` exit 0 same tree (`core-v31b.log`, `inv-v31b.log`) |
| GitHub CI on `ai-analysis` (181cf99, both runs) | **Green on the runner, 2026-09-14** — the first green CI since the v3.0.0 cut, and it covers every source change of the day: unit **637 / 0 / 5 = 642** (the parity test skips there: no Neural Engine, `open-items.md`), scientific **46 harnesses, zero FAIL**, core and inventory exit 0, on both the push and the pull-request run. Runner is `macos-26` with Xcode 26.6; two fixes got it there, both on the branch (`ContentView`'s importer closure as a method; the parity gate, refuted-and-held). Commits after 181cf99 are docs only (`git diff --stat 181cf99..HEAD -- mac4DSTEM/ mac4DSTEMTests/ tools/` empty at closeout). Closeout `inventory` exit 0 (`inventory-closeout-20260914.log`): AppState + ResultExport **7405 = HEAD^**, live markdown **6 138** — up from 6 076 at the day's start: three closed items moved to the archive, five findings recorded, two decisions written, the CI and merge paragraphs; nothing live was found stale enough to delete. |
| `run-tests.sh scientific` | **46 harnesses, 46 finished, zero `FAIL` lines, exit 0 — 2026-09-16, on the orientation-relationship-form tree** (`sci-orform-20260916.log`, `SCI_EXIT=0` on its own line); `phase-vector-matching` **32 gated checks, 0 failed**. This is the first full `scientific` run since the OR form landed in the tree, and the one the increment was committed on |
| `run-tests.sh core` (both packages) | **exit 0 — 2026-09-17**, the origin-validity-mask tree (`core-v31.log`, `CORE_EXIT=0` on its own line): `DSTEMCore` and `DSTEMSession` both build clean in 5.44 s warm. Verification-only — moves no number. Previously: exit 0 — 2026-09-17, the itemF tree (`core-itemF.log`, 14.33 s warm) |
| `tools/rotation-parity-test/run.sh` (new) | **exit 0 — 2026-09-17**, on the tree that added the `rotation` `sources.manifest` group (`rotation-parity-final-20260917.log`, `EXIT=0` on its own line): analytic recovery at 0.5° tolerance recovered a planted 37.2° to within ~3e-6° for both transpose=false and a transposed field; the py4DSTEM curl-grid-search leg hit its own documented stop condition (new open item, "RotationCalibration's py4DSTEM parity leg exposes an (Rx,Ry)-vs-(col,row) frame class") and ships informational, not gated; ADR 024's null certified 48/48 vetted planted-rotation seeds at sd≤0.03 and refused 8/8 vetted rotation-free seeds. Both break-first mutations (a channel swap; an inverted `carriesRotation` comparison) went red then `cmp`-clean green (`rotation-parity-mutation1-20260917.log`, `rotation-parity-mutation2-20260917.log`). `run-tests.sh inventory` exit 0 the same tree (`inv-A-20260917.log`) |
| `run-tests.sh inventory` | **exit 0 — 2026-09-18, AppState seam 4** (`inventory-seam4-20260918.log`): AppState + ResultExport **5277 (was 5420 after seam 3)**; AppState.swift alone 3676 lines — down from 4699 at the start of the night, four seams in. Previous: exit 0 — 2026-09-18, AppState seam 3 (`inventory-seam3-20260918.log`, AppState + ResultExport 5420, was 5831 after seam 2) (`prepush-20260918-001534.log`): the gate now also lists tracked files over 1 MiB against `tools/large-files.allow` (two today). Previous: exit 0 — 2026-09-17 (`inv-B-20260917.log`): AppState + ResultExport **7311 (was 7400)** — the first net shrink since C5's hard form was overruled, from five dead symbols deleted; live markdown **5072** (7 106 at the 2026-09-16 closeout, `inv-step3b-20260916.log`) after the docs consolidation of `0d7470d`; cold-start set **978** (2 148) |

## Handoff — 2026-09-22

| Item | State | Next step, owner |
|---|---|---|
| v3.1 Calibration foundation | Code-complete on `main`; acceptance cubes local; Friedel demo driven. | Ships with the next cut (number owed). |
| **Unverified on screen** | Owner-driven so far: the Origin-method picker; the Friedel demo (2026-09-19); the Settings scene and mp-id sheet (2026-09-21); the 2026-09-21 restyle (rejected); phase 1, ADR 035, the toolbar, the §9 rebuild (`03cfa10`); **the macOS 27 UI/UX rebuild (2026-09-22 night, a real cube: "fantastic so far")**. The pill padding and the aligned pane headers confirmed by the owner the same night. Unseen: light mode on a real cube; the Materials Project sheet's first live fetch. | — |
| Phase mapping / AI Analysis | Unvalidated by design. 2026-09-21: known-variants rule 2.64 % at floor 0; per-phase slab (θ′ 0.3) **1.81 %**, band 0.96–1.75 not yet (`theta-prime-slab-2026-09-21.md`); S3 wired (unseen). | Per-slot slab field; the Al → precipitate residual; owner drives S3. |
| R–Q rotation | ADR 024's null refuses on `sim_Au` by design (`SimpleError 1`); the parity leg's frame class is undiagnosed (`open-items.md`). | Gate D on the frame class, then `sim_Au` depth-vs-null, then the presentation fix. |
| Owner decisions owed | Parallax bin-schedule; completeness guard; release-readiness Gate-D items; 119-defect triage; release number; collapsible cards (kept for now). | Owner. |
| **AppState seams** | Plan complete 2026-09-18, all seven landed (`docs/appstate-seams-plan.md`); `AppState.swift` 1474 lines. | Owner: drive seam 7's flow. |
| **Materials Project** | S4a/S4b/S5 landed 2026-09-21 (`docs/v3-features.md#materials-project`): decode/standardise/refuse a cell, classify via CIF import; Keychain key; mp-id sheet; 39 tests. First live fetch pending. | Owner: retry mp-134, then S6. |
| Detector kernel | Detection gains worsen the matrix-by-exclusion verdict. | After the rows above. |
| **Window design reset** | Closed 2026-09-22 night: the macOS 27 rebuild on Apple's inspector guidance (flat sections, one toolbar verb, colour on symbols, contrast in the pane popovers), driven and accepted by the owner on a real cube. Record: ADR 008, 035–037, the commits `fbc59f9`..`3514566`. | Residuals in `open-items.md`: the toolbar actions' leading jump when a long file name drops the display at 915 pt; kit gaps (adaptive `Menu`, warning note); the panel overlap. |
| Repo | One branch; pre-push hook proven, run by GitHub Desktop; five v3 registrations in `docs/v3-features.md`. | Nothing owed. |

Text of record for the 2026-09-16/17 handoff: [`archive/v3/status-handoff-2026-09-18.md`](archive/v3/status-handoff-2026-09-18.md); the day's evidence stays in `open-items.md` and `archive/v3/`.

## Owed to the owner

- The §10g decisions and plan §8 (sidecar wire format). C8's engines question was settled 2026-09-08: leave (`decisions.md`).
- Four session-4 choices to overrule on sight, and the C4 slices 1-2 drive — both carried in [`archive/v3/v3.0.0-closeout-2026-09-11.md`](archive/v3/v3.0.0-closeout-2026-09-11.md).
