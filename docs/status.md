# Status

The one live status table. Updated in the same commit as the work it describes; anything older than the current step moves to `docs/archive/`. Numbers are quoted only from dated runs.

**What a log name in these rows is** (settled 2026-09-09): a log name like `unit-c7s2-20260908.log` identifies the run a number came from, not a path a reader can open — session logs live in the gitignored session scratchpad and are not retained past the session. What a reader reproduces is the gate, not the log — `tools/run-tests.sh` is the only thing that knows the harness count. Evidence a reader must be able to open is committed under `docs/archive/`, and the inventory gate fails on a repo-rooted path a truth doc cites and does not have.

## Releases

Releases: `docs/releasing.md` § Releases.

## Where the UI stands

What that train left behind is the shape the app has now — `DSTEMCore` and `DSTEMSession` packages, `DisplayedProduct`, `CalibrationSession`, `ProductWorkflow.readiness`, `ACOMSession`, five workspace sidebars. The dated step table is archived at [`archive/v3/status-steps-2026-09-16.md`](archive/v3/status-steps-2026-09-16.md).

## Last gates (retained logs)

| Gate | Result |
|---|---|
| `run-tests.sh unit` | **693 passed / 0 failed / 2 not run = 695 — 2026-09-17, the origin-validity-mask tree** (`unit-v31-full2.log`, `UNIT_EXIT=0` read off the log's own line, not the background wrapper — the caller's "exit 0" is untrusted). 695 `func test` in source, reconciled by the suffix count (`grep -o "()' passed on 'My Mac"`, this file's documented trap); **+4** over the BraggVectorEMDTypes tree (2 `CalibrationDisclosureTests` init-carry + nil-contract, 2 Metal-driven `ProbeSizeTests` production-carry), **−1** tautology removed after the adversarial review; the same two environment skips (`BookmarkResolutionLatencyTests`, `TB1StallProbeTests`). Break-first M1 (init), M2 (`appOriginMaps` bridge) and **M3 (both production carries → nil)** each went red then clean-green (exit 65); M3 is the one the first tests could not catch. `core` + `inventory` exit 0 same tree (`core-v31b.log`, `inv-v31b.log`) |
| GitHub CI on `ai-analysis` (181cf99, both runs) | **Green on the runner, 2026-09-14** — the first green CI since the v3.0.0 cut, and it covers every source change of the day: unit **637 / 0 / 5 = 642** (the parity test skips there: no Neural Engine, `open-items.md`), scientific **46 harnesses, zero FAIL**, core and inventory exit 0, on both the push and the pull-request run. Runner is `macos-26` with Xcode 26.6; two fixes got it there, both on the branch (`ContentView`'s importer closure as a method; the parity gate, refuted-and-held). Commits after 181cf99 are docs only (`git diff --stat 181cf99..HEAD -- mac4DSTEM/ mac4DSTEMTests/ tools/` empty at closeout). Closeout `inventory` exit 0 (`inventory-closeout-20260914.log`): AppState + ResultExport **7405 = HEAD^**, live markdown **6 138** — up from 6 076 at the day's start: three closed items moved to the archive, five findings recorded, two decisions written, the CI and merge paragraphs; nothing live was found stale enough to delete. |
| `run-tests.sh scientific` | **46 harnesses, 46 finished, zero `FAIL` lines, exit 0 — 2026-09-16, on the orientation-relationship-form tree** (`sci-orform-20260916.log`, `SCI_EXIT=0` on its own line); `phase-vector-matching` **32 gated checks, 0 failed**. This is the first full `scientific` run since the OR form landed in the tree, and the one the increment was committed on |
| `run-tests.sh core` (both packages) | **exit 0 — 2026-09-17**, the origin-validity-mask tree (`core-v31.log`, `CORE_EXIT=0` on its own line): `DSTEMCore` and `DSTEMSession` both build clean in 5.44 s warm. Verification-only — moves no number. Previously: exit 0 — 2026-09-17, the itemF tree (`core-itemF.log`, 14.33 s warm) |
| `tools/rotation-parity-test/run.sh` (new) | **exit 0 — 2026-09-17**, on the tree that added the `rotation` `sources.manifest` group (`rotation-parity-final-20260917.log`, `EXIT=0` on its own line): analytic recovery at 0.5° tolerance recovered a planted 37.2° to within ~3e-6° for both transpose=false and a transposed field; the py4DSTEM curl-grid-search leg hit its own documented stop condition (new open item, "RotationCalibration's py4DSTEM parity leg exposes an (Rx,Ry)-vs-(col,row) frame class") and ships informational, not gated; ADR 024's null certified 48/48 vetted planted-rotation seeds at sd≤0.03 and refused 8/8 vetted rotation-free seeds. Both break-first mutations (a channel swap; an inverted `carriesRotation` comparison) went red then `cmp`-clean green (`rotation-parity-mutation1-20260917.log`, `rotation-parity-mutation2-20260917.log`). `run-tests.sh inventory` exit 0 the same tree (`inv-A-20260917.log`) |
| `run-tests.sh inventory` | **exit 0 — 2026-09-17, the hygiene tree** (`inv-B-20260917.log`, `INV_EXIT=0` on its own line): AppState + ResultExport **7311 (was 7400)** — the first net shrink since C5's hard form was overruled, from five dead symbols deleted; live markdown **5072** (7 106 at the 2026-09-16 closeout, `inv-step3b-20260916.log`) after the docs consolidation of `0d7470d`; cold-start set **978** (2 148) |
| `tools/package-test/run.sh` | **exit 0 — 2026-09-08, the C7 session-1 tree** (`inventory-c7-final-20260908.log`): gated 46, diagnostic 9; `AppState` + `ResultExport` 7 531 (unchanged); live markdown up from 4 693 — the runtime's decisions, the plan's C7 line and one open item, against a shorter handoff; the reason is stated here. |
| `run-tests.sh all` | **exit 0 — 2026-09-11, the v3.0.0 cut gate on the frozen tree** (`all-v3cut-20260911.log`, `GATE_EXIT=0` on its own line), **46 harnesses, zero `FAIL` lines, unit 573 passed / 0 failed / 2 skipped = 575**, reconciled against **575** `func test` in source. Covers everything this session landed: the architecture pin and its fixture, the GPL/NOTICE resources, the Finder URL handler, the concurrent-open guard and its two new tests. `package-test` now prints six PASS lines, two of them new — the GPL text inside the bundle, and `arm64` alone on the executable and all three dylibs, *built the way the archive builds*. `inventory` exit 0 the same day (`inventory-final.log`): **AppState + ResultExport 7509, exactly equal to HEAD**, so C5 was paid rather than waived — the guard was compressed to two lines and two duplicate blank lines collapsed to cover it. Live markdown **4962**, down. **Reconciliation trap, and the recorded fix for it is itself wrong:** this file has said since 2026-09-09 to "count `^Test case '` lines by status". That undercounts — here by exactly one, `QCalibrationOriginGateTests.testUnusableOriginRefusesQCalibrationAndSetsNoScale`, whose result line xcodebuild glued onto the end of the preceding line so that it begins neither with `Test case '` nor with anything anchorable. Count the **suffix** instead: `grep -o "()' passed on 'My Mac"`. Reading the anchored count would have reported 572/575 and sent the next session hunting a test that had in fact passed. **Three refusals on the way here, all exit 69 at the 8 GB floor**, and the background wrapper printed "exit code 0" for every one of them — read `GATE_EXIT`, never the caller. Space came from Xcode's `DerivedData`, `tools/free-space.sh --clear`, and this session's own scratch archives. **Re-run attempted 2026-09-17** (`itemM-all.log`, `GATE_EXIT=1` on its own line, disk 10-11 GB throughout — never the exit-69 preflight): all **48** `scientific`-array harnesses (including the new `rotation-parity-test`) finished with **zero `FAIL` lines**; `real-data-acceptance`'s own `comparator-test` self-test passed 46/46; its real-cube acceptance run measured all 6 locally-present `.h5` files without error and the 2 that are also pinned goldens (`polycrystal_2D_WS2.h5`, `sim_Au_data_all_binned.h5`) matched exactly, then correctly refused with `FAIL: 3 pinned dataset(s) absent from the report` — 3 of the 5 pinned cubes are genuinely not on this machine (predicted by this item's own brief). `set -euo pipefail` stopped `all` there; `unit` and `package-test` were never reached. **Not a red verdict on the app** — every check that ran, ran clean; the recorded state is that this machine cannot complete the FULL `all` gate for want of 3 files, which re-pinning goldens (owner-only) or supplying the data would fix, not a code change |

## Handoff — PR #1 merged to `main` 2026-09-17 (`ccbbca6`), CI green; next is v3.1 (Calibration foundation)

**Docs consolidated 2026-09-16/17** (`0d7470d`, `e415929`, ADR 032): read `CLAUDE.md` → this file → `ROADMAP.md` → `docs/open-items.md` → `docs/decisions.md`. The audit that drove it is `docs/archive/audit-2026-09-16/` (its §3.2 refactor list is the code-hygiene queue in `open-items.md`). The hygiene slice's shared readiness row (`UI/CalibrationReadinessRow.swift`): **the owner drove Prepare on 2026-09-17 00:18** and his screenshot shows the readiness section rendering its five "Not set" lines; the Export-sheet half is still unseen. The same drive reported the R–Q refusal on `sim_Au` (by design, ADR 024, recorded at the 2026-09-15 drive) and a sidecar grant failure (new, Gate D open in `open-items.md`). **Deviation-note audit 2026-09-17**: 43 Core/ ported sources read-only, most already consistent; 4 undocumented deviations got a class-a `DEVIATION` note inline (`DiskDetection.swift`, `OrientationMatcher.swift`, `ProbeKernel.swift`, `StrainMapping.swift`, each verified against the pinned py4DSTEM source before writing), 5 more filed as candidates — **2026-09-17 four got their inline `DEVIATION` note** (`EllipseCalibration`, `ParallaxPreprocessing`, `OrientationPlan`, `ScatteringFactors`, refuter-checked) and the fifth (`ParallaxAlignment.swift`'s bin schedule, Science-lane) got its full Gate D diagnosis in `open-items.md`, fix still owed. Zero Core behavior changed. Nothing else below this paragraph changed.

**Where step 3 is.** **4.21 %** against the band 0.96–1.75 %, at a 0.1 % detection threshold with the shipped 70-peak cap (`thronsen-rel0.001-cap70-20260916.log`). Al 98.11 %, θ′ edge-on 70 %, face-on **99.6 %** (from 52 %), T1 89 %. The sequence is 98.26 → 26.4 → 13.24 → 8.75 → 7.96 → 6.64 → **4.21**. No shipped default moved; py4DSTEM's 0.5 % still ships, so this is the setting's number on this instrument. The demo cube is identical at 0.2 % and 0.1 %, every clause holding.

**What is closed as a lever** (`open-items.md`, full record in [`archive/v3/step3-2026-09-16.md`](archive/v3/step3-2026-09-16.md)): the 70-peak cap, the phase-contrast margin, and any threshold below 0.1 %. Do not re-open them without new evidence.

**What the next increment is.** The error at 0.1 % is **83 % refusals** (1024 of 1230), **98 % of which arrive because no candidate cleared the guards**. T1 is 617 of them. **Correction, 2026-09-17** (`docs/open-items.md`, "T1 [0 -4 1] reference measured"): the "predicts 0.233 and 0.367 Å⁻¹ reflections the data does not show" framing was itself measured and found confident-wrong — the reference the code actually builds carries no group near 0.367 at all, and sits within 0.001-0.014 Å⁻¹ of the ground truth's 0.454/0.479 at four distinct lengths, i.e. the reflections are NEAR, not absent, and arguably over-complete rather than wrong. **Measured and its Gate D experiment RUN 2026-09-17** (refuted): the 2 survivors at the 252 not-indexed T1 positions are ONE `|q|≈0.467 Å⁻¹` family — the {200} Friedel pair — but ~3° off antiparallel (`|u+v|` median 0.027 > the 0.02 pair radius), so `containsFriedelPair` fails and the floor stays 3. The reference length is correct — **do NOT change the T1 reference**. The origin hypothesis was tested and **refuted**: a per-position direct-beam origin sits 0.04 px from the global one and recovers only 24/252 (10 %), so the residual is **per-peak centroid noise on the weak {200} spots**, not origin. When a pair does clear 0.02 it correctly indexes as T1 (24/24) — the limit is detection precision. Levers (each Gate D): better centroiding, loosen the pair tolerance (recovers ~half at rising Al/cliff cost), or accept a detection-limited T1 recall. **Head-to-head 2026-09-17** ([`archive/v3/py4dstem-t1-comparison-2026-09-17.md`](archive/v3/py4dstem-t1-comparison-2026-09-17.md)): fed the SAME peaks, py4DSTEM's own ACOM + `CrystalPhase` mislabels **100 % of T1 positions as Al** (never removes the matrix, scores intensity-weighted) — a confident mislabel where mac4DSTEM abstains, so this **validates the matrix-removal design and leans the T1 lever to "accept the detection limit,"** not a defect to chase.

**CI is green on `main`** (runs 35239260526 the merge, 35239547929 `a5bb9f3`, both `success`, 2026-09-17). The pre-merge "still red on the parity test" note is **resolved**: the merged tree carries the Neural-Engine skip (`LearnedDiskDetectorTests.swift:239-242`), so `testLearnedPathMatchesPythonReference` skips rather than fails where no Neural Engine exists. Local branch `split/ci-parity-skip` is now **redundant** — it differs from `main` only in a reworded comment, not in the skip logic; safe to delete.

**On screen and owed.** The Phases panel has a new **"Matrix evidence"** row reporting the median explained fraction behind matrix verdicts, with a caption saying there is deliberately no threshold. **Nobody has looked at it.** It is stated unverified until the owner drives it. New 2026-09-17: the **origin validity mask** is on the product (`OriginMaps.originValidity`) but has **no on-screen surface yet** (v3.1 item 1 D4) — a validity overlay/count on the origin-map display is owed and unverified on screen. Everything else owed on screen from 2026-09-15 is unchanged.

**The branch.** `ai-analysis` is 52+ commits ahead of `origin/ai-analysis` and carries a merge of `origin/main` (`42597d8`) that changes no content — `git diff --stat HEAD` on the resolved merge was empty — so PR #1 is mergeable again. A peer session advised keeping both sides of the three docs conflicts; that would have duplicated three paragraphs and reverted one, and was not done.

**Merge done 2026-09-17.** PR #1 (`ai-analysis`→`main`) merged as `ccbbca6`; the September line (AI Analysis room + vector-matched phase mapping, **unvalidated by design** — every phase product self-reports `validation:"none"`) is consolidated into `main`. CI is green on the branch content (unit/scientific/core/inventory all pass on `1fc63ca`); the parity skip that made it green is already in the tree, and the ContentView type-check fix that had `main` red since the v3.0.0 cut arrived with the merge. `ai-analysis` is left in place, an ancestor of the merge; new work is on `main`.

**NEXT, in order.**
1. **v3.1 — Calibration foundation** ([`v3.1-calibration-preregistration.md`](v3.1-calibration-preregistration.md), ADR 033). **Item 1 (origin validity mask) — Core disclosure LANDED 2026-09-17**, unattended: `OriginMaps.originValidity: [Bool]?` carries the `TrimmedFit.kept` mask the robust fit already computed and dropped; **no fitted number moves** (unit 693/0/2=695, core + inventory exit 0). Wire format frozen by construction — the mask lives on `OriginMaps`, not the sidecar type `PixelOriginMaps` (D3 deferred). Break-first M1 (init)/M2 (bridge)/M3 (both production carries) each reddened the right tests. **The 4-lens adversarial pass earned its keep**: it caught that the first three tests only exercised the init assignment (M3 would have passed green) — fixed with two Metal-driven production-path tests in `ProbeSizeTests`, one tautological test removed. **Still owed: D4 (on-screen)** — a validity overlay/count is not built and nothing has been driven, so the mask is **unverified on screen**; and the step-3 "earns its place" measurement (clustered-vs-scattered exclusion, needs `Particle_1…bin8` or a synthetic). D1/D2 taken at their proposed defaults, overrule on sight (`open-items.md`). **Next buildable: item 2** — recover the discarded CoM centre in `probeSize` (`OriginCalibration.swift:516,573` keep only `probe.r`), Gate D (moves the origin) + refuter. **Diagnostic entry point established 2026-09-17** (read-only, not yet a full Gate D): py4DSTEM uses `get_probe_size`'s `(x0,y0)` as the *default origin that centers the probe cross-correlation kernel* (`braggvectors/probe.py:502-556`, `if origin is None`). But the port already centers the kernel **per scan position** on the fitted origin map (`DiskDetection.swift:587-588`), falling back to `referenceOrigin` only when no map. So the discarded CoM centre's likely correct role is the **pre-fit `referenceOrigin` seed** (what disk detection uses *before* an origin fit exists — currently the detector geometric centre), NOT a replacement for the per-position fitted origins. The Gate D owes: (a) confirm what `referenceOrigin` is today and where it comes from; (b) measure whether seeding it with the CoM centre changes detected peaks — **needs the owner's real data** (demo cube + the gitignored training cubes), so the experiment cannot run unattended. Then `get_origin_friedel`, then the vacuum probe — each pre-registered before its code.
2. **Owner decisions owed (not buildable as-is):** T1's detection-limited recall (py4DSTEM head-to-head above leans "accept"); the Parallax bin-schedule Gate D fix; the cross-phase completeness guard (candidate built + parked, `455e256`); the release-readiness Gate-D items and the 119-defect triage.
3. **The detector kernel, last** — every detection improvement worsens the matrix-by-exclusion verdict until that is fixed.

## Owed to the owner

- The §10g decisions and plan §8 (sidecar wire format). C8's engines question was settled 2026-09-08: leave (`decisions.md`).
- Four session-4 choices to overrule on sight, and the C4 slices 1-2 drive — both carried in [`archive/v3/v3.0.0-closeout-2026-09-11.md`](archive/v3/v3.0.0-closeout-2026-09-11.md).
