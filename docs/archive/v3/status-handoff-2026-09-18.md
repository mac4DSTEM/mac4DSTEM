# Status handoff of 2026-09-16/17, moved verbatim 2026-09-18

The handoff had become a 1 576-word narrative; `docs/status.md` now carries a table and this file the text of record.

## Handoff — v3.1 Calibration foundation code-complete 2026-09-17 on `main` (items 1, 3, 4 landed; 2 parked); next is its on-screen drive, the full gate, and the v3.1.0 cut

**Docs consolidated 2026-09-16/17** (`0d7470d`, `e415929`, ADR 032): read `CLAUDE.md` → this file → `ROADMAP.md` → `docs/open-items.md` → `docs/decisions.md`. The audit that drove it is `docs/archive/audit-2026-09-16/` (its §3.2 refactor list is the code-hygiene queue in `open-items.md`). The hygiene slice's shared readiness row (`UI/CalibrationReadinessRow.swift`): **the owner drove Prepare on 2026-09-17 00:18** and his screenshot shows the readiness section rendering its five "Not set" lines; the Export-sheet half is still unseen. The same drive reported the R–Q refusal on `sim_Au` (by design, ADR 024, recorded at the 2026-09-15 drive) and a sidecar grant failure (new, Gate D open in `open-items.md`). **Deviation-note audit 2026-09-17**: 43 Core/ ported sources read-only, most already consistent; 4 undocumented deviations got a class-a `DEVIATION` note inline (`DiskDetection.swift`, `OrientationMatcher.swift`, `ProbeKernel.swift`, `StrainMapping.swift`, each verified against the pinned py4DSTEM source before writing), 5 more filed as candidates — **2026-09-17 four got their inline `DEVIATION` note** (`EllipseCalibration`, `ParallaxPreprocessing`, `OrientationPlan`, `ScatteringFactors`, refuter-checked) and the fifth (`ParallaxAlignment.swift`'s bin schedule, Science-lane) got its full Gate D diagnosis in `open-items.md`, fix still owed. Zero Core behavior changed. Nothing else below this paragraph changed.

**Where step 3 is.** **4.21 %** against the band 0.96–1.75 %, at a 0.1 % detection threshold with the shipped 70-peak cap (`thronsen-rel0.001-cap70-20260916.log`). Al 98.11 %, θ′ edge-on 70 %, face-on **99.6 %** (from 52 %), T1 89 %. The sequence is 98.26 → 26.4 → 13.24 → 8.75 → 7.96 → 6.64 → **4.21**. No shipped default moved; py4DSTEM's 0.5 % still ships, so this is the setting's number on this instrument. The demo cube is identical at 0.2 % and 0.1 %, every clause holding.

**What is closed as a lever** (`open-items.md`, full record in [`archive/v3/step3-2026-09-16.md`](archive/v3/step3-2026-09-16.md)): the 70-peak cap, the phase-contrast margin, and any threshold below 0.1 %. Do not re-open them without new evidence.

**What the next increment is.** The error at 0.1 % is **83 % refusals** (1024 of 1230), **98 % of which arrive because no candidate cleared the guards**. T1 is 617 of them. **Correction, 2026-09-17** (`docs/open-items.md`, "T1 [0 -4 1] reference measured"): the "predicts 0.233 and 0.367 Å⁻¹ reflections the data does not show" framing was itself measured and found confident-wrong — the reference the code actually builds carries no group near 0.367 at all, and sits within 0.001-0.014 Å⁻¹ of the ground truth's 0.454/0.479 at four distinct lengths, i.e. the reflections are NEAR, not absent, and arguably over-complete rather than wrong. **Measured and its Gate D experiment RUN 2026-09-17** (refuted): the 2 survivors at the 252 not-indexed T1 positions are ONE `|q|≈0.467 Å⁻¹` family — the {200} Friedel pair — but ~3° off antiparallel (`|u+v|` median 0.027 > the 0.02 pair radius), so `containsFriedelPair` fails and the floor stays 3. The reference length is correct — **do NOT change the T1 reference**. The origin hypothesis was tested and **refuted**: a per-position direct-beam origin sits 0.04 px from the global one and recovers only 24/252 (10 %), so the residual is **per-peak centroid noise on the weak {200} spots**, not origin. When a pair does clear 0.02 it correctly indexes as T1 (24/24) — the limit is detection precision. Levers (each Gate D): better centroiding, loosen the pair tolerance (recovers ~half at rising Al/cliff cost), or accept a detection-limited T1 recall. **Head-to-head 2026-09-17** ([`archive/v3/py4dstem-t1-comparison-2026-09-17.md`](archive/v3/py4dstem-t1-comparison-2026-09-17.md)): fed the SAME peaks, py4DSTEM's own ACOM + `CrystalPhase` mislabels **100 % of T1 positions as Al** (never removes the matrix, scores intensity-weighted) — a confident mislabel where mac4DSTEM abstains, so this **validates the matrix-removal design and leans the T1 lever to "accept the detection limit,"** not a defect to chase.

**CI is green on `main`** (runs 35239260526 the merge, 35239547929 `a5bb9f3`, both `success`, 2026-09-17). The pre-merge "still red on the parity test" note is **resolved**: the merged tree carries the Neural-Engine skip (`LearnedDiskDetectorTests.swift:239-242`), so `testLearnedPathMatchesPythonReference` skips rather than fails where no Neural Engine exists. Local branch `split/ci-parity-skip` was **deleted 2026-09-17** in the git cleanup (`docs/archive/git-cleanup-2026-09-17.md`).

**On screen and owed.** The Phases panel has a new **"Matrix evidence"** row reporting the median explained fraction behind matrix verdicts, with a caption saying there is deliberately no threshold. **Nobody has looked at it.** It is stated unverified until the owner drives it. New 2026-09-17: the **origin validity mask** is on the product (`OriginMaps.originValidity`) but has **no on-screen surface yet** (v3.1 item 1 D4) — a validity overlay/count on the origin-map display is owed and unverified on screen. Everything else owed on screen from 2026-09-15 is unchanged.

**Merge done 2026-09-17.** PR #1 (`ai-analysis`→`main`) merged as `ccbbca6`; the September line (AI Analysis room + vector-matched phase mapping, **unvalidated by design** — every phase product self-reports `validation:"none"`) is consolidated into `main`. CI is green on the branch content (unit/scientific/core/inventory all pass on `1fc63ca`); the parity skip that made it green is already in the tree, and the ContentView type-check fix that had `main` red since the v3.0.0 cut arrived with the merge. `ai-analysis` was deleted locally in the 2026-09-17 git cleanup; all work is on `main`.

**Repo hygiene 2026-09-18** (record: `CHANGELOG.md` Unreleased, commits "Repo hygiene 1/4–4/4"). One branch everywhere since the 2026-09-17 cleanup. A **pre-push hook** (`tools/hooks/pre-push`, install once with `git config core.hooksPath tools/hooks`) runs inventory and an app-target build into `build/prepush/` and refuses a red push — proven green (71 s incremental) and REFUSED on a planted type error; whether GitHub Desktop honours `core.hooksPath` is **unverified** until the owner's next push reports "pre-push: OK". The inventory gate fails on any tracked file over 1 MiB not named in `tools/large-files.allow`. GitHub: vulnerability alerts and private reporting on, ten topics, wiki and projects off, `SECURITY.md`. All citations of the two docs deleted in the consolidation repointed (9 in markdown, 30 in source comments; archive left dated). Fourteen tools got header comments. `open-items.md` compacted to the 12-line rule with bodies moved verbatim to `archive/open-items-detail-2026-09-18.md`.

**NEXT, in order.**
1. **v3.1 — Calibration foundation** ([`v3.1-calibration-preregistration.md`](v3.1-calibration-preregistration.md), ADR 033). **Item 1 (origin validity mask) — Core disclosure LANDED 2026-09-17**, unattended: `OriginMaps.originValidity: [Bool]?` carries the `TrimmedFit.kept` mask the robust fit already computed and dropped; **no fitted number moves** (unit 693/0/2=695, core + inventory exit 0). Wire format frozen by construction — the mask lives on `OriginMaps`, not the sidecar type `PixelOriginMaps` (D3 deferred). Break-first M1 (init)/M2 (bridge)/M3 (both production carries) each reddened the right tests. **The 4-lens adversarial pass earned its keep**: it caught that the first three tests only exercised the init assignment (M3 would have passed green) — fixed with two Metal-driven production-path tests in `ProbeSizeTests`, one tautological test removed. **D4 count LANDED** (`PrepareSettings.positionsUsedValue` shows "N of M positions", unit-tested M4) — **unverified on screen**; the spatial validity overlay is a larger follow-on. **Step-3 PASSED** (`trim-sweep`, 4 cubes): excluded 0.6–15.7 %, `maxGap` 1–5 — Si-SiGe (scattered) and sim_Au (clustered, `maxGap 5`) exclude alike but differ spatially, which the scalar can't see and the mask can (hole (c)). D1/D2 at defaults, overrule on sight (`open-items.md`). **Item 2 (CoM centre) — Gate D DIAGNOSED and PARKED 2026-09-17** (marginal, two-sided: in the app the aperture is always present, so the CoM would only change the kernel centre in the aperture-vs-CoM case where the CoM can be Bragg-pulled; pre-reg doc item 2). **Item 3 (`get_origin_friedel`) — Core algorithm LANDED 2026-09-17**: `FriedelOrigin.swift` (beamstop-tolerant, opt-in/additive, changes no existing origin), gated parity harness `tools/friedel-origin-test` (recovery ≤0.03 px, py4DSTEM parity ~1e-6 px, clean + through a beamstop; break-first Mf1/Mf2). **Beamstop mask LANDED 2026-09-17** — `BeamstopMask.swift` ports py4DSTEM's `DataCube.get_beamstop_mask`, **pixel-identical to scipy on the real Au_ref beamstop cube** (1453/4096 px, 0 diff) and on a synthetic (threshold/fill/dilation mutation-caught). **Item 3 WIRED INTO THE APP 2026-09-17**: `OriginMethod` enum (`.centreOfMass`/`.friedel`), `tiledRun` gains `originMethod` + a CPU `friedelMeasuredOrigins` loop (auto-mask + per-pattern `get_origin_friedel` → same fit), `CalibrationSession.originMethod`, `calibrateOrigin` passes it, and a Prepare **"Origin method"** picker (**unverified on screen**). End-to-end unit test on a centrosymmetric cube recovers an asymmetric origin (axis-swap mutation Mw1 caught); the two new Core files added to `project.pbxproj` membership (silent-drop class). **Item 4 (vacuum probe from a separate scan) LANDED 2026-09-17**: `OriginCalibration.vacuumProbeKernel` (refuses a detector mismatch, else mean DP → probe kernel, source `.vacuumScan`), `AppState.generateVacuumProbeKernel(fromScan:)`, a MapSettings **"Vacuum Scan…"** button + importer, `ReplayPlan` refusal for the recipe; unit-tested (Mv1 caught), **importer/button unverified on screen**. **v3.1 Calibration foundation is complete** (item 2 parked). **Speed follow-ups LANDED 2026-09-17, both proven number-neutral:** Friedel full-scan (`03ca440` — precompute the mask FFTs once + parallelise the per-pattern loop; the `friedel-origin-test` parity stays ~1e-6 px) and the R–Q rotation solve (parallelise the 180-angle grid search + the 15-surrogate null across cores; a bit-exact golden dump of `RotationCalibration.solve` — `rotationRad`, `objective`, `depth`, all 15 `shuffledDepths`, `carriesRotation`, two seeds × both objectives — is byte-identical before/after, and `rotation-parity-test` re-ran exit 0). Both were multi-minute on the large Debug scans the owner drives (a 330×330 = 108,900-position field: one R–Q solve measured 143 s serial in `-Onone`, and 2.6 s → 0.8 s at `-O`); now ~3× faster and fast in Release. As with Friedel, a Release build remains the real answer for large scans. Next: the cross-phase completeness guard and T1 recall — each in `open-items.md`.
2. **Owner decisions owed (not buildable as-is):** T1's detection-limited recall (py4DSTEM head-to-head above leans "accept"); the Parallax bin-schedule Gate D fix; the cross-phase completeness guard (candidate built + parked, `455e256`); the release-readiness Gate-D items and the 119-defect triage.
3. **The detector kernel, last** — every detection improvement worsens the matrix-by-exclusion verdict until that is fixed.
