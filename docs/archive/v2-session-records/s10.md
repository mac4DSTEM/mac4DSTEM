# S10 — session record (full record; §9 keeps the stub, per M1's convention)

- [x] **S10** — 2026-08-26. **Reduced-file export.** The writer's blanket
  refusal of cropped/binned views is lifted. The lift rests on the L3
  invariant — the session calibration handed to the export is view-frame,
  the tile reads return view-frame pixels, so the two share a frame by
  construction — and the refusal is replaced by the checks that invariant
  deserves: `transformedCalibration` now THROWS on origin maps whose shape
  is not the view's scan and on any origin outside the exported detector
  (the frame-mismatch net, documented as a net, not a proof), and gained
  the **ellipse semi-axis rescale it always owed** — a/b are pixel LENGTHS
  (py4DSTEM `fit_ellipse_1D`: "the semimajor axis length"; the
  polar-elliptical consumers use them absolutely; only the BraggVectors
  correction is ratio-only), the same class as the probe radius the export
  already rescaled. The pre-S10 export left them unscaled under an export
  bin, and `tools/preprocessing-export-test` pinned that defect with an
  assertion calling them "dimensionless" — assertion corrected with the
  reasoning on the line, and that fixture's origins moved inside its own
  5×7 detector, which the new bounds net requires (they never fit; nothing
  had ever checked).

  **Provenance on the exported file** (`datacube_root`):
  `mac4dstem_derivation` — source file NAME (never the path), composed
  scan/detector offsets, total bin, exported extents; composition exact by
  `floor(floor(n/a)/b) = floor(n/(a·b))` and offsets-add — and
  `mac4dstem_replay_record` — the S5 recipe re-expressed in the exported
  file's OWN frame; absence is absence.

  **The frame mapping** (the S6-carried item, closed): `ReplayFrameTransform`
  (`viewToSource` / `viewToExport`) built on `CalibrationReReference`'s own
  primitives — the new `sourceCoordinate` is the exact affine inverse of
  `binnedCoordinate` — and `ReplayRecordFrameMap`, ONE role table for every
  key the five S5 recording sites write: positions un-bin then take the
  crop offset back; lengths and g-vector displacements ×b; per-pixel
  scales ÷b; `min_absolute_intensity` REFUSES when nonzero (the correlation
  `cc = m·|m|^(p−1)` is not intensity-normalized, and binning SUMS blocks —
  an absolute threshold has no exact image in another frame); integer
  lengths refuse inexact division; unclassified keys refuse.
  `ReplayPlanner.plan` maps-then-parses, so a detector-reduced rehearsal
  now REPLAYS at full extent with re-expressed parameters instead of
  refusing — **the release claim's "binned view re-runs unchanged" is met
  for detector-reduced rehearsals** (the S19-must-check note is satisfied);
  `.mixed`/`.unknown` still refuse wholesale. The re-referencing is said on
  both carriers (the pre-click caption and the run summary's `frameNote`) —
  never a silent substitution. `ReplayParameterFrame.detectorReduced`'s
  payload widened from `cropped: Bool` to the actual crop: two same-bin
  crops at different offsets are DIFFERENT frames and now merge to
  `.mixed` — a latent S6 trap that the lift would have armed, closed in the
  same session it became live.

  **Gate B (fresh session-tier refuter with build/test access; it applied
  and ran its own mutations) produced 8 findings; 4 fixed in-session.** The
  sharpest: (1) CONFIRMED — the exported recipe was mapped assuming the
  session record is current-view-frame, which is FALSE after a promote
  (the recipe keeps its rehearsal frame by design) or a cross-frame
  restore; the stamped numbers would have been fabricated provenance of
  positions. Fixed by the `exportableRecipe` frame guard — the recipe
  carries only when its recorded frame EQUALS the live view's, else
  refuses with the reason in the export status — extracted from behind the
  save panel into a testable function (the S1 wiring lesson: the one
  confirmed defect lived exactly in the untested inline block). (2) A
  mutation SURVIVED: `.viewToExport` positions were exercised by zero
  tests — a naive `x/b` passed all 42 targeted tests AND the fixture
  (whose recipe is pre-mapped by design); test added, mutation re-applied,
  caught. (3) A second mutation SURVIVED: the fixture's scancrop composed
  offsets were symmetric (2, 2), so a y↔x swap in
  `DataCubeDerivation.compose` passed — the S8 symmetric-constants lesson
  verbatim, in a session that had just archived it; offsets made (3, 2),
  swap re-applied, caught. (4) The reviewer CONFIRMED the ellipse judgment
  against py4DSTEM's consumers ("stronger than the diff states": the only
  ratio-only consumer is the BraggVectors correction; every polar consumer
  is absolute), independently reproduced NC2's 0.25 px signature, and
  verified the centroid arbiter is transpose-sensitive. Recorded to
  open-items with owners, not fixed: the sidecar-restore frame hole
  (`applySessionCalibration` adopts the sidecar's frame with no
  specification comparison — pre-existing, S10's lift arms its export
  half; plus the post-promote save stamping a full-extent spec beside a
  rehearsal-frame record) and the three-frame recipe composition (+ the
  unversioned `datacube_root` recipe attribute). Noted in the fixture
  header: every detector in the new fixture is square, so the bounds net's
  axis identity is covered by the sibling's 5×7 detector in the same
  aggregate. No-change with reasons: schema/min-reader for the exported
  file (not a sidecar; readers fail loudly or read it whole — checked
  against `H5Reader` discovery, sidecar recognition, and py4DSTEM).

  **Fixture:** `tools/reduced-export-test` in `scientific` — the second
  manifest-sourced runner (new `export` group in
  `tools/lib/sources.manifest`). Five cases (crop / crop+bin /
  crop+bin+export-bin / asymmetric scan-crop / full-extent regression);
  the arbiter is data-vs-metadata inside each exported file (measured beam
  centroid vs stored origin, no shared code between sides, 0.02 px gate);
  two refusal probes (R1 wrong-frame maps, R2 source-frame origin) assert
  the thrown reasons; NC1–NC7 each run live and named their line before
  reverting (8 px offset, 0.25 px bin bias, ×2 Q size, unscaled ellipse,
  silent-R2, dropped scan-offset composition, y↔x swap). Runner keeps
  stderr OPEN — NC5's first run proved a discarded stderr turns a named
  refusal into a bare exit 133.

  **Tests:** `ReplayPlanTests` gained 13 mapping/decision tests and the
  binned-rehearsal execution test was rewritten from refusal to
  re-referenced replay (asserting the exact 32.5-px half-pixel answer and
  that the recipe itself never mutates); **15 discriminating mutations
  observed failing their specific tests** (M1–M9 by the author — M2 re-run
  in isolation after M9 masked it — plus the reviewer's A/B/C/D and the
  fix-verification batch A′/B′/G′).

  **Track A on the final tree, exit codes read directly:**
  `run-tests.sh scientific` **exit 0, 37 harnesses**
  (`reduced-export-test` gating in the aggregate); `run-tests.sh unit`
  **376 passed / 1 failed, exit 65** — the 1 is the S17 sidebar
  intermittent (MCP heights 1029/945/961, byte-identical to S7's numbers;
  logged in S17's observation table); MCP `test_macos` **378 passed /
  1 failed** (same test; retired UI target skipped); zero build warnings.

  **Deviations:** (1) the cross-frame export case REFUSES (reason in the
  status line) rather than composing three frames — the composition is an
  open item, not math improvised at the end of a Gate B session; (2) the
  save-panel-driven half of `exportCalibratedDataCube` remains
  UI-untestable — the decision logic is extracted and pinned
  (`exportableRecipe`), the wiring is Track B row F1.30's; (3) the
  `preprocessing-export-test` fixture was corrected (an eval-fixture
  change forced by the new refusals — origins that never fit their own
  detector, and the ellipse assertion that pinned the defect); (4) no
  `AppState` seam extracted — §8 names none for S10 and no facade state
  was added (the run-summary note lives on the existing `ReplayRun` seam).

  **Not verified:** everything on screen (F1.30/F1.31 queued; F1.23's
  stale S10 trap note amended); a real reduced → export → py4DSTEM
  round-trip on a multi-GB cube; the sidecar-restore frame hole (recorded
  with an owner, not fixed); the promote-then-export refusal path
  end-to-end in the app (pinned at the `exportableRecipe` level only).
