# mac4DSTEM v2.5 — the consolidation train

**Date:** 2026-09-02. **Status:** proposal, owner decisions listed at the end.
**Sources:** the owner's two 2026-09-01 reviews (`mac4DSTEM-ui-review`,
`mac4DSTEM-v3-architecture-brief`, code+docs) and the 2026-09-02 read-only
session (screenshot + running app + tree measurements). This file distils
the three into one plan. It changes no code and no other doc.

## 1. Naming

- **v2.0** ships now, as is.
- **v2.5** is the internal codename for the architecture consolidation. Its
  increments release as v2.1, v2.2, … No new science.
- **v3.0** is reserved for the owner's feature plan and bumps when the first
  of those features lands on the new legs.
- Process and architecture docs are **version-free**: `docs/v3-development-process.md`
  becomes `docs/development-process.md` and replaces the current one. Only the
  contract and the status file carry a version.

## 2. Where the weight is (measured 2026-09-02, `21f3990`)

| Area | Size | Reading |
|---|---|---|
| App Swift | ~42 k lines, 0 external deps | healthy for scope |
| `App/AppState.swift` | 5 528 lines, 501 stored props, 122 methods | still owns Analyses, Calibration, DPC, Disks, Strain, ACOM, overlays |
| `UI/ContentView.swift` | 1 859 lines, 210 state reads | second god object |
| Tests + `tools/` | 26 k lines, 58 harness dirs | one bespoke harness per algorithm |
| Live docs | 19 files, 9.4 k lines (`open-items.md` 2 103) | cold-start tax |
| Archive docs | 42 files, 12.8 k lines | |
| `CLAUDE.md` | 438 lines, mostly chronology | read every session |

`Core/` imports no SwiftUI/AppKit and references `AppState` in one line
(`Core/Analysis/DatasetPreview.swift:63`, a number formatter). It is already
package-separable.

The bloat is 22 k lines of markdown around 42 k lines of code, plus two god
files. It is process weight first, code weight second.

## 3. Merged UI findings, in trust order

Stale and dropped: the colormap-control blocker (fixed and verified on
screen 2026-09-02). "Runtime" marks findings only visible in the running app.

1. **Provenance is inherited and inferred.** Restored "Quantitative" badges
   survive onto new images; unknown kinds default to quantitative
   (`App/AppState.swift:755`); missing Q units fall back to `1/nm`
   (`UI/DiffractionView.swift:174`). One defect: scientific status lives in
   strings and flags, not on the product. **This is the architectural seam.**
2. **ACOM confidence is not gated** (runtime). **Step 6b, 2026-09-03:** the
   IPF-Z map greys positions below the 10th-percentile reliability by default
   (`acomReliabilityThreshold` overrides); threshold and kept fraction travel
   in the product's provenance. Track B F1.57. The chip slider is step 7 UI.
3. **Crystal replayed as carbon with gold's lattice** (runtime). **Gate D
   2026-09-02, closed:** not a replay defect — carbon was the session's
   picker choice and replay cannot substitute Z. The gate exposed the real
   hole, a drifted lattice constant replaying under the same id, now refused
   by name (`lattice_a` in the recipe). Residual: imported-CIF id collision
   by filename (`docs/open-items.md`).
4. **Calibration state is contradictory.** **Step 4b, 2026-09-03:** one
   vocabulary (Not set / From file / Measured / Manual / From session /
   Mixed / Not quantitative) and one `CalibrationSession.Verdict` on both
   surfaces; the two conflicting rules are gone. Track B F1.55.
5. **Units and labels.** "0 kV" for an unset voltage
   (`UI/ContentView.swift:151`); "A^-1" on screen; "Categorical" chip on an
   IPF map (`App/AppState.swift:750`); hexagonal IPF legend vs colour mapping
   (earlier review, unverified here); raw result-kind strings in Results.
   All resolve when labels are formatted from typed semantics. Wording:
   "Accelerating voltage", "R–Q rotation not calibrated", "Reciprocal-space
   sampling (Δq)", "Real-space scan step (ΔR)".
6. **Phase is falsely linear.** Ptychography hidden behind parallax stages;
   completed stages not revisitable; "Reconstruction Ready" rendered as a
   disabled button. Split into **DPC & iDPC / Parallax / Single-slice
   ptychography**, shared prerequisites, separate state.
7. **Trust notices in the wrong place.** Invalidated-calibration and
   session/view-mismatch warnings hide in a closed inspector; the inspector
   shows annulus radii in Prepare and a "Diffraction" histogram beside an
   ACOM pane (runtime). Rules: warnings sit above the affected image and in
   Results; the inspector describes the focused pane only.
8. **Consistency debris.** Two Prepare subtitles
   (`App/ProductWorkflow.swift:34`, `UI/ProductWorkspaceViews.swift:359`);
   "Calibrate Rotation" vs "Measure R–Q Rotation"; "Log counts" (histogram
   y) vs "log display" (intensity); footer log duplicating Promote Run
   diagnostics; truncation at the default divider; footer "Cube" meaning the
   theoretical f32 size; absolute paths in status; A/B comparison with
   independent normalisation; mixed axis-order labels; a top-level
   Workspace menu (fold into View).
9. **Origin-fit fallback hides a NaN.** **Gate D 2026-09-03, refuted as
   filed:** the middle-threshold branch is provably unreachable, in the app
   and in py4DSTEM. The real silent path is the all-zero/NaN-first guard
   returning 1 px at the centre with no provenance; pinned by a test, fix
   owed under Gate B (`docs/open-items.md`).

Fix now on v2.0: 3 (must), 2, 6, 8; 9 is a Gate D whenever convenient. Do **not** patch 1, 4, 5, 7 on the
current facade — they are what v2.5 is for.

## 4. Architecture: agreed, and the three disagreements resolved

**Agreed by both analyses.** `AppState` → composition root and window
coordinator. `ContentView` → composition plus focused task views. A typed
task registry (id, required dataset/calibration capabilities, upstream and
produced product kinds, invalidation keys, one readiness result) replaces
readiness rules re-derived in views. `ScientificProduct` is an immutable,
self-describing value (payload, axes, value semantics, frame, calibration
snapshot, validity, provenance, algorithm record); `ProductPresentation`
holds colormap/window/overlays. Persistence serialises domain values, never
the live object graph. Retired `mac4DSTEMUITests` + `tools/ui-qc-playthrough`
are deleted first. Harnesses get one source manifest. Feature work waits
until the seams are proven. The brief's twelve anti-bloat rules and its
metrics list apply unchanged.

**D1 — first seam.** Brief: `ScientificProduct`. Session: package split.
**Both, split first.** It is mechanical (see §2), takes days, and turns
"Core has no UI knowledge" into a compile error. Three local packages:

- `DSTEMCore` — Data, Compute, Analysis, Crystal, Shaders. No UI, no session.
- `DSTEMSession` — calibration, products, recipes/replay, operation lifecycle.
- the app target.

`swift build` / `swift test` from the shell removes the DerivedData disk
floor (the exit-69 refusals) and makes agent turns cheap. `ScientificProduct`
then lands in `DSTEMSession` as the brief specifies, one product at a time.

**D2 — scale.** Brief: 6–9 months full time for the full proposal. **Take
its own "foundation" option, 12–16 weeks, and stop there:** package split,
product model with two migrated workflows, dataset/calibration/operation
owners, the task registry, the Phase split. **Defer** the HDF5/EMD writer
decomposition (2 746 tested lines behind a stable facade — wrap, don't
split) and the harness-into-package-tests move (trivial after the split).
Everything beyond ships as v3 features exercising the new model.

**D3 — ceremony.** Brief keeps all gates. **Keep Gate D unchanged** (a
written diagnosis is cheap and has paid for itself three times). **Gate B
only** for changes that alter a number in `DSTEMCore`. **Retire Gate A**
fourteen-agent fleets; one reviewer at a lower tier. **Track B**: a
five-minute drive of ≤10 rows after each user-visible slice; the full
checklist once per tag. Session records → commit message + one paragraph in
`docs/decisions.md`.

**Registry = recipe engine.** The brief's task registry and the session's
"one execution path" are the same object: the registry declares what a task
needs and produces; replay already executes tasks from recorded parameters.
The live UI runs through the same path. That deletes the live-vs-replay
duplication in `AppState` and the class of bug behind finding 3.

## 5. Sequence

| Step | Content | Gate |
|---|---|---|
| 0 | Tag v2.0. Gate D on the crystal-replay finding (3) first; fixes 2, 6, 8 as owner chooses. | D |
| 1 | Delete `mac4DSTEMUITests`, the runner, the pbxproj/xcscheme refs. Archive v2 chronology to `docs/archive/v2/`. Five live docs (§6). `CLAUDE.md` ≤ 60 lines, rules only. | build + unit |
| 2 | Package split. `DatasetPreview.swift` loses its one `AppState` call. | unit + scientific green |
| 3 | `ScientificProduct`, `ProductPresentation`, `ProductStore`. Migrate **virtual imaging** end to end: compute → display → save → reopen → export. Adapter with a stated deletion condition. | B, short Track B |
| 4 | `CalibrationSession` with task-aware readiness. Deletes the four vocabularies (finding 4) and the unit fallbacks (5). | B |
| 5 | `OperationCenter` + task registry; live and replay on one path. | B |
| 6 | Extract ACOM and Strain controllers along the existing `AppState` MARK seams (ACOM 330 lines, Strain 200, Disks 300, DPC 220, Calibration 780, Analyses 500). Reliability gating (2) lands here. | B |
| 7 | Phase split (6) and `ContentView` recomposition; inspector-follows-focus (7). | short Track B |
| 8 | **Week-eight checkpoint.** One product must be simpler across compute, display, export, save, reopen. If not, revise the model before migrating more. | owner |

Second product migration after virtual imaging: **DPC** (units, frame, and
the angle-unit history all exercise the product model).

## 6. Live docs after step 1

```
CLAUDE.md / AGENTS.md         rules and routing only, ≤ 60 lines
docs/status.md                active work only, ≤ 100 lines, one table
docs/v2.5-plan.md             this file — the contract and the sequence
docs/open-items.md            unresolved items only, ≤ 500 lines
docs/development-process.md   the former v3-development-process.md, renamed
docs/architecture.md          layers, ownership, placement, target, ≤ 300 lines
docs/decisions.md             append-only, one paragraph per decision
docs/visual-acceptance-checklist.md
docs/archive/v2/…             everything else, unchanged, indexed by a README
```

Landed 2026-09-02 (step 1). A separate contract file was dropped: this plan
is the contract.

One fact has one home. Completion moves narrative to the archive at once.
Cold-start reading set ≤ 500 lines.

## 7. Working rules for the train

- Exploration on Haiku; implementation on the default model; one refuter,
  only for `DSTEMCore` numbers.
- Every session nets negative markdown lines or says why.
- No file over 800 lines lands without a split plan.
- No new stored state in `AppState`; a feature names its owner before it
  adds a field.
- No view derives units, frame, validity or provenance from strings.
- An architectural task deletes the path it supersedes; adapters carry an
  expiry condition.
- Record the §2 metrics at step 1 and after steps 3, 5, 7.

## 8. Owner decisions

1. Sidecar compatibility: read all v2 sidecars, write only the new format?
2. Second product migration: DPC (recommended) or strain?
3. Does the hexagonal-IPF-legend claim get its own Gate D, or ride with the
   crystal-replay one?
4. Does the ten-row Track B replace the full checklist for the whole train,
   or only for architecture slices?
5. Which v3 feature requests are refused until step 8 (a visible list
   protects the train)?

## 9. Step 3 pre-registration — the virtual-imaging product (written 2026-09-02)

**a. Parallel fields today (one virtual-imaging result, spread across `AppState`)**

| Field | file:line | Holds |
|---|---|---|
| `resultImage` | AppState.swift:245 | pixel payload (`FloatImage`) |
| `resultRGBA` | AppState.swift:253 | alt payload when categorical (nil for virtual imaging) |
| `resultColormap` | AppState.swift:993 | presentation, not product |
| `displayRangeLo/Hi`, `resultGamma` | AppState.swift:1081-1083 | presentation, not product |
| `restoredResultInfo` (kind, name, units) | AppState.swift:268 | set only when reopened; must clear on fresh publish |
| `restoredResultPixelInfo`, `restoredResultDomain` | AppState.swift:269-271 | reopened sampling/frame/provenance |
| `navigationResultInfo/PixelInfo/Domain` | AppState.swift:274-278 | task-switch relabel cache, cleared by `resultImage`'s `didSet` |
| `currentResultKind/DisplayName/ValueUnits` | ResultExport.swift:1699-1709 | computed fallback chain: restored ?? navigation ?? live-mode switch |
| `currentScalarResultMetadata` | ResultExport.swift:1389 | per-`analysisMode` kind/name/units switch; `.virtualDetector` case at 1392-1394 |
| `currentResultPersistenceMetadata` | ResultExport.swift:1711 | sampling+units+provenance, folds in `display_domain`/`quantitative_status` |
| `activeResultDomain` | AppState.swift:737 | `.scan` for `.virtualDetector` (741) |
| `quantitativeStatus(for:units:)` | AppState.swift:745 | unknown kind → `.quantitative` at 755 (the "unknown defaults to quantitative" trap) |
| `DisplayedProduct` | Core/Data/DisplayedProduct.swift:49 | existing partial `ScientificProduct` — assembled fresh per read by `AppState.displayedProduct` (AppState.swift:671), not stored |

**b. Five surfaces, entry points**

- Compute: `AppState.runVirtualDetector` AppState.swift:3472; publish block 3555-3576 (`restoredResultInfo=nil`, `resultImage=image`, `scanNavigationImage=image`, `resultVersion+=1`, `recordReplayStep`).
- Display: `StemImageView` UI/StemImageView.swift — reads `app.displayedProduct` (53,86,135,190,214,344,364) and `app.displayedResult*` (43-50); badge derivation 127-171.
- Save: `AppState.saveCurrentResultToSessionSidecar` ResultExport.swift:739, via `currentScalarResultMapForPersistence()` (711) → `ScalarResultMap` (Core/Data/BraggVectorEMDWriter.swift:7) → `BraggVectorEMDWriter.mergeResultMap` (798).
- Reopen: `AppState.restoreSessionResult` AppState.swift:3013, called from 2674; also `selectSavedSessionResult` ResultExport.swift:840-869 (both set the `restored*` triple, never `navigationResult*`).
- Export: `AppState.exportResultImage` ResultExport.swift:173 — reads **raw** `resultRGBA`/`resultImage`/`resultColormap`/`displayRangeLo/Hi`/`resultGamma` (179-186), not `displayedResult*`, so it can export the wrong payload under `showsACOMRegionReference`; `publicationCaption` (277) reads `displayedProduct` instead — the two halves of one export already disagree on source.

**c. Invariants (§4, restated)**

- Payload, axes, value semantics, frame, calibration snapshot, validity, provenance, algorithm record travel together as one immutable value.
- Presentation (colormap/window/overlays) is a separate value, never folded into the product.
- Persistence serialises the domain value, never the live object graph.
- No view derives units, frame, validity or provenance from strings.

**d. Adapter plan**

1. `ScientificProduct` replaces the compute-side publish (a): `runVirtualDetector` constructs it directly instead of setting `resultImage`/`restoredResultInfo=nil` separately. Deletion condition: once every `analysisMode` publishes a `ScientificProduct`, delete `resultImage`/`resultRGBA`/`restoredResultInfo`/`navigationResultInfo` and their didSet coupling.
2. `AppState.displayedProduct` (671) becomes the accessor that unwraps the stored `ScientificProduct`, not an assembler — `currentScalarResultMetadata`'s per-mode switch (ResultExport.swift:1389) moves into each compute call site. Deletion condition: `currentScalarResultMetadata` has zero call sites.
3. `exportResultImage` reads the same `ScientificProduct` as `publicationCaption` already does. Deletion condition: `exportResultImage`'s raw-field branch (179-186) is gone; one source for both halves.
4. Sidecar read/write take a `ScientificProduct` codec instead of `ScalarResultMap`'s discrete fields. Deletion condition: `ScalarResultMap`/`RGBAResultMap` become the codec's wire format only, never constructed outside `BraggVectorEMDWriter`.

**e. Tests to write BEFORE the change (negative controls)**

- Publish a fresh virtual-detector result while `restoredResultInfo` is non-nil (simulating a missed clear); assert the new product's `kind`/`displayName` are the fresh ones, not the restored ones — must FAIL today if the `restoredResultInfo=nil` line at AppState.swift:3555 is removed.
- Feed `quantitativeStatus(for:units:)` (AppState.swift:745) a kind string not in its switch; assert it does NOT return `.quantitative` — must FAIL against the current default at line 755.
- Set `calibration.qPixelSize` non-nil, `qPixelUnits` nil, and read the unit label; assert it is NOT `"1/nm"` — must FAIL against DiffractionView.swift:174.

**f. Owner-only**

- Whether `ScalarResultMap`/`RGBAResultMap` stay as the sidecar wire format permanently or are themselves replaced (relates to §8 decision 1, sidecar compatibility).
- Whether `exportResultImage`'s ACOM-region-reference bug (b, Export) is fixed now, ahead of step 3, or folds into the migration.

## 10. Step 5 pre-registration — the task registry and one execution path (written 2026-09-03)

**a. Tasks (kind, entry point, requirements as checked today, upstream, produced)**

| kind | entry point | required (blocking) | non-blocking guidance | upstream | produced kind(s) |
|---|---|---|---|---|---|
| `virtual_detector` | `runVirtualDetector` AppState.swift:3494 | none (`ProductWorkflow.swift:267-268`) | none | dataset | `virtual_<shape>` (3586) |
| `dpc` | `runDPC` AppState.swift:4409 | none | origin/rotation/Q/R/voltage (`ProductWorkflow.swift:342-350`); iDPC sub-mode also gated by `SessionGates.originQuantitativeRefusal` via `idpcPhysicalCalibration` (4600,4612) | dataset | `dpc_magnitude`/`_mrad`/`_angle`/`_color`/idpc variant (`applyDPCDisplay` 4504) |
| `disk_detection` | `runDiskDetection` AppState.swift:4734 | none in `ProductWorkflow` (`.disks`→`[]`, 267-268); param validity enforced only inside the function (4745-4753) | — | dataset + probe kernel (auto-generated, 4736) | `bragg_vector_map` (4882, via `showBraggMap` 4877) |
| `strain` | `runStrainMapping` AppState.swift:4924 | `hasBraggVectors` (`ProductWorkflow.swift:296-301`); re-checked as `diskDetectionSettingsAreStale` inside the function (4926) | `hasOriginProbe` (351-354) | `braggVectors` | `strain_exx/eyy/exy/theta/fit_residual/indexed` (5108-5113, via `applyStrainDisplay` 5102) |
| `acom` | `runACOM` AppState.swift:5301 | `hasBraggVectors` + `hasACOMMaterial && hasSupportedACOMMaterial` (`ProductWorkflow.swift:303-319`); re-derived inside the function as `diskDetectionSettingsAreStale` (5303), `resolvedACOMModel` (5313), `orientationPlan` generation (5319-5322) | `hasPhysicalACOMScale` (355-358) | `braggVectors` + crystal selection | `acom_<scope>_<field>` (5457, via `applyACOMDisplay` 5437) |

Not in the recipe vocabulary at all: ptychography/parallax (`prepareParallaxPreview`/`alignParallaxNextLevel`/`fitParallaxAberrations`/`correctParallaxPhase`/`upsampleParallaxBF`, dispatched only from `runPrimaryWorkspaceTask`'s `.reconstruct` case, AppState.swift:1446-1459) — no `kind` string, no `ReplayStepPlan` case; replay cannot reach them today.

**b. Prerequisite re-derivation sites**

| file:line | what it checks |
|---|---|
| AppState.swift:1082-1097 | `productWorkflowReadiness` — the one derivation of `ProductWorkflowReadiness` from `calibrationSession`/`hasCurrentBraggVectors`/ACOM fields |
| ProductWorkflow.swift:262-321 | `prerequisiteItems(for:readiness:)` — pure per-task mapping, consumed at 4 sites below |
| TaskPrerequisiteChecklist.swift:64-69 | checklist panel rows |
| ProductWorkspaceViews.swift:271-276, 423-430 | header divider gate; `primaryActionEnabled` (Run button) |
| ContentView.swift:1501-1505, 914-917 | sidebar task glyph unmet-count; Reconstruct sidebar "Still needed" text |
| **DiskDetectionControls.swift:275** | `diskDetectionConfigurationIsValid` — a SIXTH, independent disable for the tools-panel "Detect All Disks" button; `.disks` has no `prerequisiteItems` at all, so this can disagree with the header's `primaryActionEnabled` for the identical task |
| AppState.swift:4745-4753 | `runDiskDetection` re-derives param validity a second time at execution, same predicate as above, no shared gate |
| AppState.swift:1982-2009 (`executeReplayStep` `.acom`) | a SEVENTH derivation: `ACOMReplayPlan.resolveMaterial` (by id) + a ±1e-6 scale-tolerance check with no equivalent in `ProductWorkflowReadiness` |
| AppState.swift:1950-1962 (`executeReplayStep` `.dpc`) | compares `calibration.hasFittedOrigin` to the recorded flag; unrelated to `ProductWorkflow`'s DPC guidance list or to `SessionGates` |
| Session/SessionGates.swift:55,79 | an eighth axis — "may this result be called quantitative," orthogonal to "may this task run" |

**c. AnalysisOperationController vs. AppState**

| Owns | Where |
|---|---|
| Token identity, name, totalUnits, `hasActiveOperation`, `begin/finish/isCurrent/cancelCurrent/reset`, elapsed/rate/ETA | `AnalysisOperationController` (88 lines, `Core/Workflow/`) |
| `isBusy`, `statusText`, `progress`, `datasetEpoch` (1117), `datasetLoadCancellation/Progress/Status`, `AnalysisRunOutcome` (96-104, never touched by the controller), the glue wrappers `beginCancellableOperation/finishCancellableOperation/updateCancellableOperation/cancelActiveOperation` (1272-1318) | `AppState.swift` |

`operationController.reset()` is called directly from dataset-lifecycle sites (2406, 2845) that bypass `finish()` entirely — a second way an operation ends, outside the token protocol.

**d. Live vs. replay**

- Parameter source: replay WRITES the same mutable fields (`aperture`, `diskParams`, `strain.*`, `acomModelSelection/Scope/Quality`) `executeReplayStep` reads them from, then calls the identical run function with `replaying: true` — no second compute path.
- Readiness: live is gated BEFORE the call by `primaryActionEnabled` (button disabled); the run function's internal guards are defense-in-depth. Replay has no `primaryActionEnabled` equivalent — only the bespoke per-kind guards in (b) (dpc, acom only; virtual_detector/disk_detection/strain have none), then the same internal guards fire again.
- Refusal wording: live uses `ProductWorkflow.prerequisiteItems` titles / `presentComputeFailure` strings; replay writes distinct recipe-voiced sentences (e.g. "the recipe ran DPC against calibrated origins…") — two independent copywriting sources for related conditions.
- Recording: `recordReplayStep` fires only on success and only `!replaying` (212-219); cancellation/failure never records.
- `quiet:` (auto-run on open, 3498) is a third mode sharing the run function but not `replaying`'s recipe semantics.

**e. Proposed shape**

```swift
struct TaskDescriptor {
    let kind: AnalysisMode
    let requires: [TaskPrerequisite.Resolution: (ProductWorkflowReadiness) -> Bool]
    let guidance: (ProductWorkflowReadiness) -> [String]
    let upstreamKinds: [String]
    let producedKinds: (Params) -> [String]
}
enum Readiness { case ready, unavailable(reason: String), current }
protocol TaskRegistry {
    func readiness(for: AnalysisMode, in: ProductWorkflowReadiness) -> Readiness
}
// checklist, primaryActionEnabled, sidebar glyph, and executeReplayStep
// all call registry.readiness(for:in:) — replay adds only its own
// recorded-vs-live parameter comparison, never a second requirements list.
```

**f. Tests to write BEFORE the change**

- Disk detection: assert `disk.detectAll` (tools panel) and `workspace.primaryAction` (header) report the same disabled state across a range of invalid `diskParams` — must FAIL today (`diskDetectionConfigurationIsValid` is outside `ProductWorkflowReadiness`).
- ACOM: pin the CURRENT divergence between a live run's "Exploratory matching only…" guidance and a replay's "matched at %.6g Å⁻¹/px…" refusal for the same off-physical-scale condition, so the registry's unification shows as a diff, not a silent change.
- For every `AnalysisMode`, assert `TaskPrerequisiteChecklist`'s unmet-items and `primaryActionEnabled`'s block state are computed from one `registry.readiness` call, not two.

**g. Owner-only**

- Whether ptychography/parallax join the recipe vocabulary now or wait for step 7's Phase split.
- Whether `diskDetectionConfigurationIsValid` becomes a `TaskDescriptor` requirement (disables the header button too) or stays tools-panel-only.
- Whether `SessionGates`' quantitative-claim refusals fold into `readiness(for:)` or stay a second, separate axis.
- Whether replay keeps its recipe-voiced refusal text or adopts the checklist's `TaskPrerequisite.title` vocabulary once both share one call.

## 11. Step 7 pre-registration — Phase split, inspector focus, recomposition (written 2026-09-03)

**Already landed, out of scope here:** 7d (`06bca78`) put the ACOM reliability slider on
`ColormapChipMenu`'s `.result` popover (`ColormapChipMenu.swift:76-100`, bound to
`appState.acomReliabilityThreshold`) — item (e) of the original brief is done; not re-registered.

**a. Phase tasks today**

| Task | Entry point | Depends on | Gates it | Hides it |
|---|---|---|---|---|
| DPC & iDPC | `runCurrentAnalysis` via `runPrimaryWorkspaceTask` `.reconstruct`/`.dpc` AppState.swift:1509-1510 | nothing blocking (`prerequisiteItems` returns `[]` for `.dpc`, ProductWorkflow.swift:270-271) | none (only non-blocking `guidance`, ProductWorkflow.swift:376-384) | nothing — its sidebar controls sit outside the "Reconstruction workflow" `Section`, which is gated `.ptychography`-only (ContentView.swift:480) |
| Parallax (5 stages: preprocess/align/fit/correct/upsample) | `prepareParallaxPreview`/`alignParallaxNextLevel`/`fitParallaxAberrations`/`correctParallaxPhase`/`upsampleParallaxBF`, dispatched only from `runPrimaryWorkspaceTask` `.reconstruct` AppState.swift:1511-1520 | `.ptychography`'s shared calibration prerequisites (origin/rotation/Q/R/voltage, ProductWorkflow.swift:288-313) | each stage's button disables on the prior stage's nil-check (e.g. `parallaxPreprocess == nil`, ContentView.swift:497-501) | `reconstructionStage()` (ContentView.swift:1561-1589) renders `content()` only when `active == currentReconstructionStage` (1583) — R21's flat rows show ONE stage; a complete stage's controls (e.g. "Reset Alignment", 505-509) vanish once the next stage becomes active. `reconstructionStageBinding` (1608-1612) is already dead — zero call sites |
| Single-slice ptychography | `runSingleslicePtychography` AppState.swift:4012 | only `reader`/`fourD`/`descriptor` + `ParallaxPhysicalCalibration.resolve` (calibration+aperture+voltage, 4020-4030) — **never reads `parallaxPreprocess`/`Alignment`/`Correction`** | no dedicated `AnalysisMode`; folded into `.ptychography`'s same calibration list | its `DisclosureGroup` (ContentView.swift:624-696, "Reconstruct Object" button 688-693) is nested INSIDE `reconstructionStage(4, …)`'s content closure (545-697) — invisible until stages 1-3 are complete, though it needs none of them. `runPrimaryWorkspaceTask`'s `.reconstruct` case never calls it at all (1508-1521); the header's primary action (`primaryActionTitle` ProductWorkspaceViews.swift:391-398) walks only the 5-stage chain, ending "Reconstruction Ready" (398) — disabled once `parallaxCorrection` AND `parallaxSubpixel` are both set (`primaryActionEnabled` 432-436) |

**b. Proposed split** (plan §3 item 6: "shared prerequisites, separate state")

- **DPC & iDPC** — unchanged; already independent.
- **Parallax** — keeps its real sequential dependency (alignment needs preprocess, correction needs the fit, upsample needs alignment); the fix is presentation only — every completed stage stays visible with its controls (drop the `active`-only gate at ContentView.swift:1583), not just the current one.
- **Single-slice ptychography** — becomes its own task: same shared calibration prerequisites as Parallax (`.ptychography`'s current list), zero dependency on parallax stage state. Its controls and "Reconstruct Object" move out of stage 4's `DisclosureGroup` into their own always-visible task view, wired through the header primary action like every other task (`WorkspaceArea.reconstruct.analysisModes`, ProductWorkflow.swift:65, becomes three entries; every `.ptychography`-only branch in `primaryActionTitle`/`primaryActionEnabled` splits three ways).

**c. Inspector today**

| Section | Condition | State read |
|---|---|---|
| Dataset (DatasetInspector.swift:34) | `descriptor != nil` (16) | descriptor, `datasetPreview`, `loadedView`, replay record |
| Live (165) | same — unconditional beyond a loaded dataset | `selectedScan`, `patternMinMax`, **Aperture** (174-178, an origin/Prepare concept), result histogram (180-188), **diffraction histogram** (190-203) — none gated on `navigation.workspaceArea`/`analysisMode` |
| Diagnostics (211) | same | `replayRun`, `loadedView.invalidatedCalibration`, `lastRotationResult`, `PerformanceView`, session-provenance mismatch, sidecar-unreadable/doesNotFit |
| Products (20, `ProductsView`) | same | calibration flags, `hasCurrentBraggVectors`, `strain.map`, `hasOrientationMap`, `sessionInventory` |

All four render together whenever a dataset is loaded (DatasetInspector.swift:14-28) — this is finding 7 verbatim: Aperture and the diffraction histogram show in every workspace, ACOM included.

**Focus today**: only `AppState.activePane: ActivePane` (`.diffraction`/`.realSpace`, AppState.swift:1084; set at ContentView.swift:1378,1385; drawn as `paneFocusBorder` at 1182,1376,1383) — it distinguishes the two canvases inside the Prepare/virtual-imaging view only. No equivalent exists for Map's disk/strain/IPF panes, Reconstruct's DPC/Parallax/Ptycho panes, or Results.

**Rule after**: one descriptor per pane, naming which of the four groups above (and which subsection — e.g. Aperture only for Prepare) applies; the inspector renders the focused pane's descriptor. Requires generalizing `ActivePane` past its two cases (or replacing it) — owner call, see (g).

**d. ContentView recomposition**

- 15 `Section`s, 210 `appState.` reads (ContentView.swift, current tree) split along the boundaries already used for gating (e.g. 480, 935): `PrepareSidebar`/`ImageSidebar`/`MapSidebar` (Disks/Strain/ACOM)/`PhaseSidebar` (DPC/Parallax/Ptycho, per (b))/`ResultsSidebar`.
- Forwarders → direct session reads: `appState.calibration`/`calibrationProvenance`/`originFitFunction`/`ellipseFit*` (23 UI sites; forwarder block AppState.swift:308-329, comment "these forwarders keep the readers compiling until they move there") → `appState.calibrationSession.*`. `appState.acomModelSelection`/`acomReliabilityThreshold`/etc. (16 UI sites; forwarder block from 484, comment at 484-485) → `appState.acomSession.*`. `appState.strain.*` (9 sites) already reads `StrainProduct` directly (188) — the model for the other two.
- Deletion condition, each forwarder block: zero remaining `appState.<forwardedName>` sites outside `AppState.swift` itself.
- `reconstructionStageBinding` (ContentView.swift:1608-1612): delete now, already unreferenced.

**e.** Slider already landed (see top) — no action owed.

**f. Tests to write BEFORE**

- Switching Phase task (DPC ↔ Parallax ↔ Ptycho) never clears another task's own state — must FAIL if a future `changeMode` gains a reset side effect (AppState.swift:1466-1475 today touches none).
- With `parallaxCorrection == nil`, single-slice ptychography's controls are visible — must FAIL today (ContentView.swift:1583 hides stage 4's content while an earlier stage is active).
- With stage 2 complete and stage 3 active, stage 2's "Reset Alignment" (504-510) stays reachable — must FAIL today (only the active stage renders).
- With `navigation.analysisMode == .acom`, `DatasetInspector`'s Live group renders neither the Aperture rows (174-178) nor the diffraction histogram (190-203) — must FAIL today (no such condition exists).
- Each of DPC / Parallax / Single-slice ptychography reports an independent `primaryActionTitle`/`primaryActionEnabled` — must FAIL today (ProductWorkspaceViews.swift:391-398, 432-436 is one five-branch chain for two of the three).

**g. Owner decisions — taken 2026-09-03 (`docs/decisions.md`)**

- Naming as coded (DPC & iDPC / Parallax / Single-slice ptychography); ADV on both ptychography tasks.
- Results gets its own inspector (product descriptor + session inventory + the Diagnostics group).
- `ActivePane` stays for the ROI direction; a separate `FocusedPane` value, set by the pane with the focus ring and read only by the inspector, replaces the per-workspace conditions.
- The run functions move into their session objects within 7c, as each family's last slice, after that family's sidebar reads the session directly; the forwarder block goes in the same commit. *Amended 2026-09-03: the runs stay on `AppState`; the sessions own the state and its invalidation (`decisions.md`).*

**h. 7c slice order (one commit each, short drive after each)**

1. `ResultsSidebar` + the Results inspector (`ProductInspector`), `FocusedPane` introduced for the Results pane.
2. `PrepareSidebar` reading `calibrationSession`; Prepare's panes set `FocusedPane`; the inspector renders from it.
3. `ImageSidebar` (virtual detector + DPC? no — DPC is Phase) reading `calibrationSession`/`operationCenter`.
4. `MapSidebar` (disks, strain, ACOM) reading `acomSession`/`strain`; then `runACOM`/`applyACOMDisplay`/`runStrainMapping` and the orientation plan/map move into `ACOMSession`/`StrainProduct`; the ACOM forwarder block deleted.
5. `PhaseSidebar` (DPC & iDPC / Parallax / Single-slice ptychography); the calibration forwarder block deleted when its last reader is gone.
6. `ContentView` is composition only; `reconstructionStageBinding` and `openedReconstructionStage` deleted (already unreferenced).
