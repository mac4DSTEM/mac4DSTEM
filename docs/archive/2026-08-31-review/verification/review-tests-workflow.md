# Recovered tests/workflow review — 2026-08-31

Independent source review covers every original entry assigned here: **22/22** across app-rest (6), tests (8), and tools-gates (8). Five session records audited: S17, S18, S21, W4a, W4b. No builds, tests, mutations, app interaction, commits or shared code edits.

Statuses: confirmed 8, narrowed 12, refuted 1, resolved 1. Severities: low 11, medium 9, none 2. These are review dispositions, not reproduced bug counts.

## Findings that survive

The actionable medium findings are the wrong iDPC remedy substring, absent recovery dataset identity check (durable impact still needs reproduction), vacuous strain equivalence/clear-wiring tests, tautological parity frame round-trip, silent LoadView nil skip, overclaimed parallel harness coverage, CoM's shared-kernel oracle gap, and direct rotation/preparation coverage gaps. Documentation and brittle smoke-check concerns are low severity. README roster drift was already resolved by Claude.

## Important refutations/corrections

- P→P.T is a no-op for the parity test's symmetric P. The assertion is tautological, but this proposed mutant proves nothing.
- PtychographyPreparer **is called** by AppState; searching only the filename/type guessed from it missed the live caller.
- Production parallel disk detection runs in other gates; disk-correlation-parity's default is serial and its manual parallel branch is copied work splitting.
- CoM runs in three gated harnesses, not one. All found expected outputs nevertheless share the kernel.
- ACOM polar inputs and templates are normalized. The demo test does not use an unbounded raw score.
- A template-derived radial-deposition fixture is valid for its explicitly scoped matcher test; it was not claiming crystal validation.
- Successful recent-file opening refreshes the stored bookmark again inside active security scope, refuting the permanent-never-repaired narrative.
- The sidebar is hidden during loading. Recovery-write code is unguarded, but initial slider-reachability and durable-loss claims require a controlled experiment.

## Audit evidence and remaining limits

W4b's retained scientific.log independently counts **41 started, 41 completed, zero FAIL/SKIP, EXIT=0**. App source was byte-identical to W4a at review. This does not cover the parent's newly added ACOM harness. Other historical numeric runs/mutations and remote CI outcomes are not disproven, but were not independently replayed or all re-counted here.

S18's missing Gate B was cleared later in commit 8eab0e4; the old Not verified paragraph cannot override the dated follow-up. Its counter/equality overclaims were already corrected in open-items. F1.45 needs 'lower' threshold wording and an explicit distinction between defective-scale operational inspection and the corrected-scale basal expectations; parent notified.

## Process consequence

The original overall completion notification was fail-open: 11 finder results existed, only 2 verification areas finished, and 3 audit batches failed. Retain stable findings, explicit child completion/error states, and source-revision-linked evidence. Never substitute source plausibility for a recorded reproduction or mutation run.

Artifacts: verified-tests-workflow.json and audit-s17-w4b.json in this directory. Parent-created fixture work is intentionally outside this original-snapshot review.
